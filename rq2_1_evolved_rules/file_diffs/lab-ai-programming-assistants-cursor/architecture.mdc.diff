commit 5ac4092242cb0bc9645be49995b6494a1faae575
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Mon May 5 23:07:41 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Mon May 5 23:07:41 2025 +0800

    rule: added project architecture rules and UI mockup image
    
    - Created a new architecture rules document outlining front-end and back-end technology standards, including best practices for React, Node.js, and TypeScript.
    - Added a mockup image for the user interface to the project assets.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
new file mode 100644
index 0000000..246af3b
--- /dev/null
+++ b/.cursor/rules/architecture.mdc
@@ -0,0 +1,64 @@
+---
+description: 
+globs: 
+alwaysApply: true
+---
+# Project Architecture Rule (方案一：React + Vite + TypeScript + Node.js/Express)
+
+> **Rule Type:** Always
+
+## 架构概述
+- 前端：React 18 + Vite + TypeScript + Tailwind CSS
+- 后端：Node.js + Express + TypeScript
+- 前后端完全分离，接口通过 RESTful API 通信
+- 后端负责代理 DeepSeek API，前端不直接暴露 API Key
+- 统一使用 TypeScript 保证类型安全
+- 代码风格、目录结构、依赖管理、接口规范等均需遵循下述标准
+
+---
+
+## 前端技术标准（2025 最佳实践）
+- 使用 React 18 函数组件和 Hooks，禁止使用类组件
+- 状态管理优先使用 React Context + useReducer，复杂场景可用 Zustand/Recoil
+- 路由管理使用 React Router v6
+- 样式优先使用 Tailwind CSS，禁止全局 CSS，组件内可用 CSS Modules
+- 组件拆分粒度适中，复用性强，禁止在组件内直接操作 DOM
+- API 请求统一封装（如 axios/fetch），所有请求类型定义于 `src/types/api.ts`
+- 严格使用 TypeScript，所有 props、state、API 数据均需类型声明
+- 目录结构建议：
+  - `src/components/` 复用组件
+  - `src/pages/` 页面组件
+  - `src/hooks/` 自定义 Hook
+  - `src/types/` 类型定义
+  - `src/utils/` 工具函数
+  - `src/services/` API 封装
+- 代码风格遵循 Prettier + ESLint（airbnb 或官方推荐规则）
+- 单元测试优先使用 Vitest + React Testing Library
+- 国际化、历史记录等功能后续章节实现，预留扩展点
+
+## 后端技术标准（2025 最佳实践）
+- 使用 Node.js 20+，Express 5+，TypeScript 5+
+- 路由、控制器、服务、类型分层清晰，目录结构建议：
+  - `src/routes/` 路由定义
+  - `src/controllers/` 控制器
+  - `src/services/` 业务逻辑
+  - `src/types/` 类型定义
+  - `src/utils/` 工具函数
+- 所有 API 接口严格定义输入输出类型，统一错误处理
+- API 路径风格遵循 RESTful 规范，返回 JSON
+- 环境变量管理使用 dotenv，敏感信息不入库
+- 日志记录使用 winston/pino，开发与生产分级
+- 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
+- 单元测试优先使用 Vitest/Jest + supertest
+- 仅暴露必要 API，前端不可直接访问 DeepSeek API Key
+
+## 通用规范
+- 依赖管理使用 pnpm/yarn/npm，锁定依赖版本
+- 代码提交前必须通过 lint、type check、单元测试
+- 代码注释清晰，重要逻辑需中英文注释
+- 统一接口文档（如 OpenAPI/Swagger），便于前后端协作
+- 预留后续功能扩展点（如 i18n、历史记录、用户系统等）
+
+---
+
+> 本规则为本项目架构与技术标准的唯一权威来源，后续所有开发、讨论、代码生成均需严格遵循本文件内容。


commit 46250cae7d8dc25f667e172a3cc9908c2c72191c
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Mon May 5 23:46:16 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Mon May 5 23:46:16 2025 +0800

    rules: added .gitignore and updated architecture rules for testing frameworks
    
    - Added a .gitignore file to exclude common build artifacts, environment files, and editor configurations.
    - Updated architecture rules to specify preferred testing frameworks for both front-end and back-end, emphasizing the use of Vitest and Jest for unit testing.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
index 246af3b..2882d05 100644
--- a/.cursor/rules/architecture.mdc
+++ b/.cursor/rules/architecture.mdc
@@ -33,7 +33,7 @@ alwaysApply: true
   - `src/utils/` 工具函数
   - `src/services/` API 封装
 - 代码风格遵循 Prettier + ESLint（airbnb 或官方推荐规则）
-- 单元测试优先使用 Vitest + React Testing Library
+- 单元测试优先使用 Vitest（2025年主流，速度快，兼容性好）+ React Testing Library，支持组件和逻辑的高效测试
 - 国际化、历史记录等功能后续章节实现，预留扩展点
 
 ## 后端技术标准（2025 最佳实践）
@@ -49,7 +49,7 @@ alwaysApply: true
 - 环境变量管理使用 dotenv，敏感信息不入库
 - 日志记录使用 winston/pino，开发与生产分级
 - 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
-- 单元测试优先使用 Vitest/Jest + supertest
+- 单元测试优先使用 Jest（2025年依然主流，生态完善，支持依赖注入）、Vitest（如需更快的测试体验），集成 supertest 进行 API 测试；推荐结合 ts-jest、jest-mock-extended 或 typedi/inversify 等依赖注入库，确保服务、控制器等可独立测试
 - 仅暴露必要 API，前端不可直接访问 DeepSeek API Key
 
 ## 通用规范


commit 2f6ca77f5d260e54bb0e2b6c9e2b73e75139a840
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Tue May 6 09:04:15 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Tue May 6 09:04:15 2025 +0800

    rules: picked jtest (not vitest) for backend unit testing
    
    - Updated the architecture document to clarify the preferred use of Jest for unit testing, emphasizing its mainstream status in 2025 and support for dependency injection. Adjusted the wording for better readability and removed redundancy regarding testing libraries.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
index 2882d05..8c258ae 100644
--- a/.cursor/rules/architecture.mdc
+++ b/.cursor/rules/architecture.mdc
@@ -49,7 +49,7 @@ alwaysApply: true
 - 环境变量管理使用 dotenv，敏感信息不入库
 - 日志记录使用 winston/pino，开发与生产分级
 - 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
-- 单元测试优先使用 Jest（2025年依然主流，生态完善，支持依赖注入）、Vitest（如需更快的测试体验），集成 supertest 进行 API 测试；推荐结合 ts-jest、jest-mock-extended 或 typedi/inversify 等依赖注入库，确保服务、控制器等可独立测试
+- 单元测试优先使用 Jest（2025年依然主流，生态完善，支持依赖注入），集成 supertest 进行 API 测试；推荐结合 ts-jest、jest-mock-extended 与 typedi/inversify 等依赖注入库，确保服务、控制器等可独立测试
 - 仅暴露必要 API，前端不可直接访问 DeepSeek API Key
 
 ## 通用规范


commit 7b10127533a81626515e9b2301d544c3e66e9b36
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Tue May 6 16:33:01 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Tue May 6 16:33:01 2025 +0800

    rules: expanded architecture document with C4 model details
    
    - Added comprehensive C4 model architecture points for the Promptyoo-0 project, detailing the Context, Containers, and Components layers for both backend and frontend. This includes descriptions of system interactions, component breakdowns, and development environment specifications.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
index 8c258ae..32e4350 100644
--- a/.cursor/rules/architecture.mdc
+++ b/.cursor/rules/architecture.mdc
@@ -62,3 +62,65 @@ alwaysApply: true
 ---
 
 > 本规则为本项目架构与技术标准的唯一权威来源，后续所有开发、讨论、代码生成均需严格遵循本文件内容。
+
+---
+
+# Promptyoo-0 C4 Model 架构要点
+
+## 1. Context 层（系统上下文）
+- Promptyoo-0 是一个网页端提示词优化工具，帮助用户通过回答6个问题生成高质量AI提示词。
+- 主要参与者：
+  - 最终用户：通过浏览器与系统交互，获取优化提示词。
+  - DeepSeek API：外部AI服务，由后端安全代理调用。
+- 前端与后端完全分离，前端不直接暴露API Key。
+
+## 2. Containers 层（系统容器）
+- **前端 Web 应用（React + Vite + TypeScript + Tailwind CSS）**
+  - 用户界面、表单交互、结果展示，通过RESTful API与后端通信。
+- **后端服务（Node.js + Express + TypeScript）**
+  - 处理前端请求，校验输入，代理DeepSeek API，返回优化提示词。
+- **DeepSeek API（外部AI服务）**
+  - 由后端调用，生成优化后的提示词。
+- **本地运行环境**
+  - 前后端均可本地开发，依赖管理与环境变量规范化。
+
+## 3. Components 层（组件层，Backend & Frontend）
+
+### Backend 组件拆解
+- **API 路由层（src/routes/）**：定义RESTful API路由。
+- **控制器层（src/controllers/）**：处理API请求逻辑，参数校验、调用服务层。
+- **服务层（src/services/）**：封装与DeepSeek API的交互逻辑。
+- **类型定义层（src/types/）**：API输入输出类型声明。
+- **工具函数层（src/utils/）**：通用工具、参数校验、日志等。
+- **环境变量与配置管理**：dotenv管理敏感信息。
+- **日志与错误处理**：winston/pino日志，统一错误响应。
+- **单元测试与接口测试**：Jest + supertest，保证各层可测。
+- **API文档生成**：如Swagger/OpenAPI，便于协作。
+- **扩展点**：历史记录、国际化、用户系统等。
+
+### Frontend 组件拆解
+- **页面与路由层（src/pages/）**
+  - HomePage：项目介绍、6问表单。
+  - ResultPage：展示优化结果。
+  - NotFoundPage：未匹配路由处理。
+- **复用组件层（src/components/）**
+  - QuestionForm：6问输入表单。
+  - PromptResult：优化结果展示。
+  - LoadingSpinner/ErrorMessage/Header/Footer等。
+- **状态管理与上下文（src/context/ 或 src/hooks/）**
+  - PromptContext + usePromptReducer：全局状态管理。
+  - usePromptApi/useCopyToClipboard等自定义Hook。
+- **API服务封装（src/services/）**
+  - promptService.ts：与后端API交互。
+- **类型定义（src/types/）**
+  - api.ts/form.ts：请求响应、表单、props等类型。
+- **样式与UI规范**
+  - Tailwind CSS优先，组件内可用CSS Modules。
+- **工具函数（src/utils/）**
+  - 表单校验、格式化等。
+- **单元测试（src/__tests__/）**
+  - Vitest + React Testing Library，组件和逻辑测试。
+- **扩展点**
+  - 国际化、历史记录、主题切换等。
+
+---


commit 17851af71b0e539f1c996b6cb1ba01f7e8433664
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Tue May 6 20:59:30 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Tue May 6 20:59:30 2025 +0800

    rules: enhanced architecture document with detailed C4 component diagrams for Promptyoo-0
    
    - Added C4 component diagrams for both request and response flows, outlining the interactions between frontend and backend components, including user interactions, API requests, and data handling processes.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
index 32e4350..807f755 100644
--- a/.cursor/rules/architecture.mdc
+++ b/.cursor/rules/architecture.mdc
@@ -124,3 +124,71 @@ alwaysApply: true
   - 国际化、历史记录、主题切换等。
 
 ---
+
+C4Component
+title Promptyoo-0 请求流程图 - Components 层
+
+Person(user, "终端用户", "通过浏览器访问应用")
+
+Container_Boundary(frontend, "前端 Web 应用") {
+    Component(homePage, "HomePage", "React", "包含6问表单的主页")
+    Component(questionForm, "QuestionForm", "React", "6个问题的输入表单")
+    Component(promptContext, "PromptContext", "React Context", "管理表单状态")
+    Component(promptService, "promptService", "TypeScript", "封装API请求")
+}
+
+Container_Boundary(backend, "后端服务") {
+    Component(routes, "API路由层", "Express Router", "定义API端点")
+    Component(controllers, "控制器层", "TypeScript", "处理请求、校验参数")
+    Component(services, "服务层", "TypeScript", "封装业务逻辑")
+    Component(utils, "工具函数层", "TypeScript", "参数校验、格式化等")
+}
+
+System_Ext(deepseekAPI, "DeepSeek API", "外部AI服务")
+
+Rel(user, homePage, "访问", "HTTP")
+Rel(homePage, questionForm, "包含", "")
+Rel(questionForm, promptContext, "更新状态", "")
+Rel(promptContext, promptService, "提交表单数据", "")
+Rel(promptService, routes, "发送API请求", "HTTP POST")
+Rel(routes, controllers, "路由到", "")
+Rel(controllers, utils, "验证参数", "")
+Rel(controllers, services, "调用", "")
+Rel(services, deepseekAPI, "请求生成优化提示词", "HTTP")
+
+---
+
+C4Component
+title Promptyoo-0 响应流程图 - Components 层
+
+Person(user, "终端用户", "通过浏览器访问应用")
+
+Container_Boundary(frontend, "前端 Web 应用") {
+    Component(homePage, "HomePage", "React", "将根据状态重定向")
+    Component(resultPage, "ResultPage", "React", "展示优化结果")
+    Component(promptResult, "PromptResult", "React", "显示优化提示词组件")
+    Component(promptContext, "PromptContext", "React Context", "存储响应数据")
+    Component(promptService, "promptService", "TypeScript", "处理API响应")
+}
+
+Container_Boundary(backend, "后端服务") {
+    Component(services, "服务层", "TypeScript", "处理AI响应数据")
+    Component(controllers, "控制器层", "TypeScript", "格式化响应数据")
+    Component(routes, "API路由层", "Express Router", "返回HTTP响应")
+    Component(utils, "工具函数层", "TypeScript", "错误处理、日志记录")
+}
+
+System_Ext(deepseekAPI, "DeepSeek API", "外部AI服务")
+
+Rel(deepseekAPI, services, "返回优化提示词", "HTTP Response")
+Rel(services, controllers, "处理后的AI响应", "")
+Rel(controllers, utils, "记录响应", "")
+Rel(controllers, routes, "格式化后的响应数据", "")
+Rel(routes, promptService, "返回API响应", "HTTP JSON")
+Rel(promptService, promptContext, "更新状态", "")
+Rel(promptContext, homePage, "触发路由更新", "")
+Rel(homePage, resultPage, "重定向至", "React Router")
+Rel(resultPage, promptResult, "渲染", "")
+Rel(promptResult, user, "展示优化后的提示词", "UI")
+
+---


commit a127c1fb1c7dc31f96f2f1584d2a81b3a7a07bf5
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Wed May 7 12:24:41 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Wed May 7 12:24:41 2025 +0800

    chore: updated .gitignore to include rules folder and remove architecture.mdc file for regeneration
    
    - Modified .gitignore to not ignore rules in the .cursor/rules folder.
    - Deleted the architecture.mdc file, which contained detailed project architecture rules and C4 diagrams.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
deleted file mode 100644
index 807f755..0000000
--- a/.cursor/rules/architecture.mdc
+++ /dev/null
@@ -1,194 +0,0 @@
----
-description: 
-globs: 
-alwaysApply: true
----
-# Project Architecture Rule (方案一：React + Vite + TypeScript + Node.js/Express)
-
-> **Rule Type:** Always
-
-## 架构概述
-- 前端：React 18 + Vite + TypeScript + Tailwind CSS
-- 后端：Node.js + Express + TypeScript
-- 前后端完全分离，接口通过 RESTful API 通信
-- 后端负责代理 DeepSeek API，前端不直接暴露 API Key
-- 统一使用 TypeScript 保证类型安全
-- 代码风格、目录结构、依赖管理、接口规范等均需遵循下述标准
-
----
-
-## 前端技术标准（2025 最佳实践）
-- 使用 React 18 函数组件和 Hooks，禁止使用类组件
-- 状态管理优先使用 React Context + useReducer，复杂场景可用 Zustand/Recoil
-- 路由管理使用 React Router v6
-- 样式优先使用 Tailwind CSS，禁止全局 CSS，组件内可用 CSS Modules
-- 组件拆分粒度适中，复用性强，禁止在组件内直接操作 DOM
-- API 请求统一封装（如 axios/fetch），所有请求类型定义于 `src/types/api.ts`
-- 严格使用 TypeScript，所有 props、state、API 数据均需类型声明
-- 目录结构建议：
-  - `src/components/` 复用组件
-  - `src/pages/` 页面组件
-  - `src/hooks/` 自定义 Hook
-  - `src/types/` 类型定义
-  - `src/utils/` 工具函数
-  - `src/services/` API 封装
-- 代码风格遵循 Prettier + ESLint（airbnb 或官方推荐规则）
-- 单元测试优先使用 Vitest（2025年主流，速度快，兼容性好）+ React Testing Library，支持组件和逻辑的高效测试
-- 国际化、历史记录等功能后续章节实现，预留扩展点
-
-## 后端技术标准（2025 最佳实践）
-- 使用 Node.js 20+，Express 5+，TypeScript 5+
-- 路由、控制器、服务、类型分层清晰，目录结构建议：
-  - `src/routes/` 路由定义
-  - `src/controllers/` 控制器
-  - `src/services/` 业务逻辑
-  - `src/types/` 类型定义
-  - `src/utils/` 工具函数
-- 所有 API 接口严格定义输入输出类型，统一错误处理
-- API 路径风格遵循 RESTful 规范，返回 JSON
-- 环境变量管理使用 dotenv，敏感信息不入库
-- 日志记录使用 winston/pino，开发与生产分级
-- 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
-- 单元测试优先使用 Jest（2025年依然主流，生态完善，支持依赖注入），集成 supertest 进行 API 测试；推荐结合 ts-jest、jest-mock-extended 与 typedi/inversify 等依赖注入库，确保服务、控制器等可独立测试
-- 仅暴露必要 API，前端不可直接访问 DeepSeek API Key
-
-## 通用规范
-- 依赖管理使用 pnpm/yarn/npm，锁定依赖版本
-- 代码提交前必须通过 lint、type check、单元测试
-- 代码注释清晰，重要逻辑需中英文注释
-- 统一接口文档（如 OpenAPI/Swagger），便于前后端协作
-- 预留后续功能扩展点（如 i18n、历史记录、用户系统等）
-
----
-
-> 本规则为本项目架构与技术标准的唯一权威来源，后续所有开发、讨论、代码生成均需严格遵循本文件内容。
-
----
-
-# Promptyoo-0 C4 Model 架构要点
-
-## 1. Context 层（系统上下文）
-- Promptyoo-0 是一个网页端提示词优化工具，帮助用户通过回答6个问题生成高质量AI提示词。
-- 主要参与者：
-  - 最终用户：通过浏览器与系统交互，获取优化提示词。
-  - DeepSeek API：外部AI服务，由后端安全代理调用。
-- 前端与后端完全分离，前端不直接暴露API Key。
-
-## 2. Containers 层（系统容器）
-- **前端 Web 应用（React + Vite + TypeScript + Tailwind CSS）**
-  - 用户界面、表单交互、结果展示，通过RESTful API与后端通信。
-- **后端服务（Node.js + Express + TypeScript）**
-  - 处理前端请求，校验输入，代理DeepSeek API，返回优化提示词。
-- **DeepSeek API（外部AI服务）**
-  - 由后端调用，生成优化后的提示词。
-- **本地运行环境**
-  - 前后端均可本地开发，依赖管理与环境变量规范化。
-
-## 3. Components 层（组件层，Backend & Frontend）
-
-### Backend 组件拆解
-- **API 路由层（src/routes/）**：定义RESTful API路由。
-- **控制器层（src/controllers/）**：处理API请求逻辑，参数校验、调用服务层。
-- **服务层（src/services/）**：封装与DeepSeek API的交互逻辑。
-- **类型定义层（src/types/）**：API输入输出类型声明。
-- **工具函数层（src/utils/）**：通用工具、参数校验、日志等。
-- **环境变量与配置管理**：dotenv管理敏感信息。
-- **日志与错误处理**：winston/pino日志，统一错误响应。
-- **单元测试与接口测试**：Jest + supertest，保证各层可测。
-- **API文档生成**：如Swagger/OpenAPI，便于协作。
-- **扩展点**：历史记录、国际化、用户系统等。
-
-### Frontend 组件拆解
-- **页面与路由层（src/pages/）**
-  - HomePage：项目介绍、6问表单。
-  - ResultPage：展示优化结果。
-  - NotFoundPage：未匹配路由处理。
-- **复用组件层（src/components/）**
-  - QuestionForm：6问输入表单。
-  - PromptResult：优化结果展示。
-  - LoadingSpinner/ErrorMessage/Header/Footer等。
-- **状态管理与上下文（src/context/ 或 src/hooks/）**
-  - PromptContext + usePromptReducer：全局状态管理。
-  - usePromptApi/useCopyToClipboard等自定义Hook。
-- **API服务封装（src/services/）**
-  - promptService.ts：与后端API交互。
-- **类型定义（src/types/）**
-  - api.ts/form.ts：请求响应、表单、props等类型。
-- **样式与UI规范**
-  - Tailwind CSS优先，组件内可用CSS Modules。
-- **工具函数（src/utils/）**
-  - 表单校验、格式化等。
-- **单元测试（src/__tests__/）**
-  - Vitest + React Testing Library，组件和逻辑测试。
-- **扩展点**
-  - 国际化、历史记录、主题切换等。
-
----
-
-C4Component
-title Promptyoo-0 请求流程图 - Components 层
-
-Person(user, "终端用户", "通过浏览器访问应用")
-
-Container_Boundary(frontend, "前端 Web 应用") {
-    Component(homePage, "HomePage", "React", "包含6问表单的主页")
-    Component(questionForm, "QuestionForm", "React", "6个问题的输入表单")
-    Component(promptContext, "PromptContext", "React Context", "管理表单状态")
-    Component(promptService, "promptService", "TypeScript", "封装API请求")
-}
-
-Container_Boundary(backend, "后端服务") {
-    Component(routes, "API路由层", "Express Router", "定义API端点")
-    Component(controllers, "控制器层", "TypeScript", "处理请求、校验参数")
-    Component(services, "服务层", "TypeScript", "封装业务逻辑")
-    Component(utils, "工具函数层", "TypeScript", "参数校验、格式化等")
-}
-
-System_Ext(deepseekAPI, "DeepSeek API", "外部AI服务")
-
-Rel(user, homePage, "访问", "HTTP")
-Rel(homePage, questionForm, "包含", "")
-Rel(questionForm, promptContext, "更新状态", "")
-Rel(promptContext, promptService, "提交表单数据", "")
-Rel(promptService, routes, "发送API请求", "HTTP POST")
-Rel(routes, controllers, "路由到", "")
-Rel(controllers, utils, "验证参数", "")
-Rel(controllers, services, "调用", "")
-Rel(services, deepseekAPI, "请求生成优化提示词", "HTTP")
-
----
-
-C4Component
-title Promptyoo-0 响应流程图 - Components 层
-
-Person(user, "终端用户", "通过浏览器访问应用")
-
-Container_Boundary(frontend, "前端 Web 应用") {
-    Component(homePage, "HomePage", "React", "将根据状态重定向")
-    Component(resultPage, "ResultPage", "React", "展示优化结果")
-    Component(promptResult, "PromptResult", "React", "显示优化提示词组件")
-    Component(promptContext, "PromptContext", "React Context", "存储响应数据")
-    Component(promptService, "promptService", "TypeScript", "处理API响应")
-}
-
-Container_Boundary(backend, "后端服务") {
-    Component(services, "服务层", "TypeScript", "处理AI响应数据")
-    Component(controllers, "控制器层", "TypeScript", "格式化响应数据")
-    Component(routes, "API路由层", "Express Router", "返回HTTP响应")
-    Component(utils, "工具函数层", "TypeScript", "错误处理、日志记录")
-}
-
-System_Ext(deepseekAPI, "DeepSeek API", "外部AI服务")
-
-Rel(deepseekAPI, services, "返回优化提示词", "HTTP Response")
-Rel(services, controllers, "处理后的AI响应", "")
-Rel(controllers, utils, "记录响应", "")
-Rel(controllers, routes, "格式化后的响应数据", "")
-Rel(routes, promptService, "返回API响应", "HTTP JSON")
-Rel(promptService, promptContext, "更新状态", "")
-Rel(promptContext, homePage, "触发路由更新", "")
-Rel(homePage, resultPage, "重定向至", "React Router")
-Rel(resultPage, promptResult, "渲染", "")
-Rel(promptResult, user, "展示优化后的提示词", "UI")
-
----


commit d386c4f840b04cd123f3eca00aec8ffe9b702169
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Wed May 7 13:13:00 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Wed May 7 13:13:00 2025 +0800

    rules: reintroduced architecture document with comprehensive project standards
    
    - Added a new architecture.mdc file detailing the project architecture and technology standards for both frontend and backend, including best practices for React, Node.js, and TypeScript.
    - Emphasized the importance of following these standards for all future development and discussions.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
new file mode 100644
index 0000000..49dfe97
--- /dev/null
+++ b/.cursor/rules/architecture.mdc
@@ -0,0 +1,77 @@
+---
+description:
+globs:
+alwaysApply: true
+---
+# Project Architecture Rule (方案一：React + Vite + TypeScript + Node.js/Express)
+
+## 架构概述
+- 前端：React 18.x + Vite 5.x + TypeScript 5.x + Tailwind CSS 4.x
+- 后端：Node.js 20.x + Express 5.x + TypeScript 5.x
+- 前后端完全分离，接口通过 RESTful API 通信
+- 后端负责代理 AI API，前端不直接暴露 API Key
+- 统一使用 TypeScript 保证类型安全
+- 代码风格、目录结构、依赖管理、接口规范等均需遵循下述标准
+
+## 前端技术标准（2025 最佳实践）
+- 使用 React 18.x 函数组件和 Hooks，禁止使用类组件
+- 状态管理优先使用 React Context + useReducer，复杂场景可用 React Query 5.x
+- 路由管理使用 React Router v6.x
+- 样式优先使用 Tailwind CSS 4.x，禁止全局 CSS，组件内可用 CSS Modules
+- 组件拆分粒度适中，复用性强，禁止在组件内直接操作 DOM
+- API 请求统一使用 React Query 5.x，所有请求类型定义于 `src/types/api.ts`
+- 严格使用 TypeScript 5.x，所有 props、state、API 数据均需类型声明
+- 目录结构建议：
+  - `src/components/` 复用组件
+  - `src/pages/` 页面组件
+  - `src/hooks/` 自定义 Hook
+  - `src/types/` 类型定义
+  - `src/utils/` 工具函数
+  - `src/services/` API 封装
+- 代码风格遵循 Prettier + ESLint（airbnb 或官方推荐规则）
+- 单元测试使用 Vitest + React Testing Library，支持组件和逻辑的高效测试
+- 性能优化使用 React.memo, useCallback, useMemo
+
+## 后端技术标准（2025 最佳实践）
+- 使用 Node.js 20.x LTS，Express 5.x，TypeScript 5.x
+- 路由、控制器、服务、类型分层清晰，目录结构建议：
+  - `src/routes/` 路由定义
+  - `src/controllers/` 控制器
+  - `src/services/` 业务逻辑
+  - `src/middleware/` 中间件
+  - `src/types/` 类型定义
+  - `src/utils/` 工具函数
+- 所有 API 接口严格定义输入输出类型，统一错误处理
+- API 路径风格遵循 RESTful 规范，返回 JSON
+- 环境变量管理使用 dotenv，敏感信息不入库
+- 日志记录使用 pino/winston，开发与生产分级
+- 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
+- 单元测试使用 Jest 30.x，集成 supertest 进行 API 测试
+- 安全实践：
+  - 使用 helmet.js 设置安全头
+  - 实现 API 速率限制
+  - 正确配置 CORS
+  - 所有用户输入验证和净化
+
+## 通用规范
+- 依赖管理使用 pnpm/yarn/npm，锁定依赖版本
+- 代码提交前必须通过 lint、type check、单元测试
+- 代码注释清晰，重要逻辑需注释
+- 统一接口文档（OpenAPI/Swagger 3.1），便于前后端协作
+- 遵循 Conventional Commits 标准提交代码
+- 使用 Husky 进行预提交钩子检查
+
+## 开发工作流
+- 特性分支工作流
+- 要求代码审查
+- CI/CD 自动化测试和部署
+- 使用 Dependabot 进行依赖更新
+
+## AI 集成指南
+- 使用最新版本 API（视实际情况而定）
+- 实现带版本控制的提示模板
+- 适当缓存 API 响应
+- 实现 AI 服务错误的适当处理
+- 速率限制和成本管理策略
+
+> 本规则为本项目架构与技术标准的唯一权威来源，后续所有开发、讨论、代码生成均需严格遵循本文件内容。


commit a89d9854b830ac030aeca3eea6f3ee1f341114db
Author:     Bin Wu <wubin28@gmail.com>
AuthorDate: Wed May 7 13:24:30 2025 +0800
Commit:     Bin Wu <wubin28@gmail.com>
CommitDate: Wed May 7 13:24:30 2025 +0800

    rules: updated architecture document with dependency injection recommendations
    
    - Added TypeScript support through ts-jest 5.x.
    - Recommended dependency injection technologies including TypeDI, InversifyJS, and tsyringe.
    - Included guidelines for service isolation testing and ensuring independent testability of controllers and services.

diff --git a/.cursor/rules/architecture.mdc b/.cursor/rules/architecture.mdc
index 49dfe97..4b42109 100644
--- a/.cursor/rules/architecture.mdc
+++ b/.cursor/rules/architecture.mdc
@@ -47,6 +47,12 @@ alwaysApply: true
 - 日志记录使用 pino/winston，开发与生产分级
 - 代码风格遵循 Prettier + ESLint（airbnb/base 或官方推荐规则）
 - 单元测试使用 Jest 30.x，集成 supertest 进行 API 测试
+  - 使用 ts-jest 5.x 提供 TypeScript 支持
+  - 依赖注入技术栈推荐：
+    - TypeDI 0.15.x / InversifyJS 7.x / tsyringe 5.x 作为依赖注入容器
+    - jest-mock-extended 4.x 提供类型安全的 mock 创建
+    - 使用接口定义 + 实现类 + DI容器 模式进行服务隔离测试
+    - 控制器和服务层必须可独立测试，避免紧耦合
 - 安全实践：
   - 使用 helmet.js 设置安全头
   - 实现 API 速率限制


