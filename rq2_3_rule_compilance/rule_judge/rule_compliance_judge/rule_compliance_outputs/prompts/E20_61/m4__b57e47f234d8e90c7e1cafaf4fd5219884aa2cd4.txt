- Rule Primary Category: Code Implementation
- Rule Secondary Category: Business Logic
- Rule File Path: .cursor/email-verification.cursorrules
- Change Type: added
- Target Rule Content:
POST /api/auth/resend-verification - Resend verification code

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: Source code files containing API route definitions, controllers, or endpoint mappings (e.g., Express router files, Spring Controller classes, FastAPI route decorators, etc.).
- Suggested Logic: Scan the source code for static route definitions that match the HTTP method 'POST' and the URL path '/api/auth/resend-verification' using Regex or AST parsing.
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=4)
- Commit Message: Fix minor bugs
- Changed Files List: 
app/src/components/TodoForm.jsx	+36	-4	after_lines=243
app/src/components/TodoItem.jsx	+34	-5	after_lines=315
app/src/components/layout/Header.jsx	+5	-2	after_lines=40
app/src/context/AuthContext.jsx	+11	-2	after_lines=150

- Project Directory Tree (Snapshot after commit):
├── .cursor
│   └── todo.cursorrules
├── .cursorrules
├── .gitignore
├── README.md
├── app
│   ├── README.md
│   ├── package-lock.json
│   ├── package.json
│   ├── public
│   │   ├── favicon.ico
│   │   ├── index.html
│   │   ├── logo192.png
│   │   ├── logo512.png
│   │   ├── manifest.json
│   │   └── robots.txt
│   └── src
│       ├── App.css
│       ├── App.js
│       ├── App.test.js
│       ├── components
│       │   ├── TodoForm.css
│       │   ├── TodoForm.jsx
│       │   ├── TodoItem.css
│       │   ├── TodoItem.jsx
│       │   ├── TodoList.css
│       │   ├── TodoList.jsx
│       │   ├── auth
│       │   │   ├── AuthForms.css
│       │   │   ├── EmailVerificationForm.jsx
│       │   │   ├── LoginForm.jsx
│       │   │   ├── ProtectedRoute.jsx
│       │   │   └── RegisterForm.jsx
│       │   └── layout
│       │       ├── Header.css
│       │       └── Header.jsx
│       ├── context
│       │   ├── AuthContext.jsx
│       │   └── TodoContext.jsx
│       ├── hooks
│       │   └── useTodos.js
│       ├── index.css
│       ├── index.js
│       ├── logo.svg
│       ├── pages
│       │   ├── EmailVerificationPage.jsx
│       │   ├── HomePage.css
│       │   ├── HomePage.jsx
│       │   ├── LoginSuccessPage.css
│       │   ├── LoginSuccessPage.jsx
│       │   ├── TodoPage.css
│       │   └── TodoPage.jsx
│       ├── reportWebVitals.js
│       ├── services
│       │   ├── apiClient.js
│       │   ├── authService.js
│       │   └── todoService.js
│       └── setupTests.js
└── backend
    ├── HELP.md
    ├── build.gradle
    ├── gradle
    │   └── wrapper
    │       ├── gradle-wrapper.jar
    │       └── gradle-wrapper.properties
    ├── gradlew
    ├── gradlew.bat
    ├── settings.gradle
    └── src
        ├── main
        │   ├── java
        │   │   └── com
        │   │       └── example
        │   │           └── todoapp
        │   │               ├── TodoApplication.java
        │   │               ├── config
        │   │               │   ├── DataInitializer.java
        │   │               │   ├── EmailConfig.java
        │   │               │   ├── JwtConfig.java
        │   │               │   └── SecurityConfig.java
        │   │               ├── controller
        │   │               │   ├── AuthController.java
        │   │               │   ├── HealthController.java
        │   │               │   └── TodoController.java
        │   │               ├── dto
        │   │               │   ├── TodoDeadlineStatsDto.java
        │   │               │   ├── TodoFilterDto.java
        │   │               │   ├── TodoRequestDto.java
        │   │               │   ├── TodoResponseDto.java
        │   │               │   ├── auth
        │   │               │   │   ├── AuthResponseDto.java
        │   │               │   │   ├── EmailVerificationRequestDto.java
        │   │               │   │   ├── LoginRequestDto.java
        │   │               │   │   ├── RegisterRequestDto.java
        │   │               │   │   ├── ResendVerificationRequestDto.java
        │   │               │   │   └── UserProfileDto.java
        │   │               │   └── common
        │   │               │       ├── ApiResponseDto.java
        │   │               │       └── ErrorResponseDto.java
        │   │               ├── entity
        │   │               │   ├── EmailVerification.java
        │   │               │   ├── Role.java
        │   │               │   ├── Todo.java
        │   │               │   └── User.java
        │   │               ├── exception
        │   │               │   ├── AuthorizationException.java
        │   │               │   ├── GlobalExceptionHandler.java
        │   │               │   └── ResourceNotFoundException.java
        │   │               ├── repository
        │   │               │   ├── EmailVerificationRepository.java
        │   │               │   ├── RoleRepository.java
        │   │               │   ├── TodoRepository.java
        │   │               │   └── UserRepository.java
        │   │               ├── security
        │   │               │   ├── CustomUserDetailsService.java
        │   │               │   ├── JwtAuthenticationEntryPoint.java
        │   │               │   ├── JwtAuthenticationFilter.java
        │   │               │   └── UserPrincipal.java
        │   │               ├── service
        │   │               │   ├── AuthService.java
        │   │               │   ├── EmailService.java
        │   │               │   ├── EmailVerificationService.java
        │   │               │   ├── JwtService.java
        │   │               │   ├── TodoService.java
        │   │               │   └── impl
        │   │               │       ├── AuthServiceImpl.java
        │   │               │       ├── EmailServiceImpl.java
        │   │               │       ├── EmailVerificationServiceImpl.java
        │   │               │       ├── JwtServiceImpl.java
        │   │               │       └── TodoServiceImpl.java
        │   │               └── util
        │   │                   ├── DateTimeUtil.java
        │   │                   ├── SecurityUtil.java
        │   │                   └── VerificationCodeGenerator.java
        │   └── resources
        │       ├── application.properties
        │       ├── data.sql
        │       ├── schema.sql
        │       └── templates
        │           ├── password-reset-email.html
        │           ├── verification-email.html
        │           └── welcome-email.html
        └── test
            └── java
                └── com
                    └── example
                        └── todoapp
                            └── TodoApplicationTests.java

- File Diffs (Code Changes in this Commit):
--- FILE: app/src/components/TodoForm.jsx ---
diff --git a/app/src/components/TodoForm.jsx b/app/src/components/TodoForm.jsx
index ff77452..81d6f93 100644
--- a/app/src/components/TodoForm.jsx
+++ b/app/src/components/TodoForm.jsx
@@ -6,10 +6,39 @@ const TodoForm = ({ onSubmit, loading, initialData = null, isEdit = false }) =>
         title: initialData?.title || '',
         description: initialData?.description || '',
         completed: initialData?.completed || false,
-        deadline: initialData?.deadline ? initialData.deadline.slice(0, 16) : '' // Format for datetime-local input
+        deadline: initialData?.deadline ? formatDateForInput(initialData.deadline) : '' // Format for datetime-local input
     });
     const [errors, setErrors] = useState({});
 
+    // Helper function to format date for datetime-local input (preserves local timezone)
+    function formatDateForInput(dateString) {
+        if (!dateString) return '';
+        const date = new Date(dateString);
+        // Convert to local timezone and format for datetime-local input
+        const year = date.getFullYear();
+        const month = String(date.getMonth() + 1).padStart(2, '0');
+        const day = String(date.getDate()).padStart(2, '0');
+        const hours = String(date.getHours()).padStart(2, '0');
+        const minutes = String(date.getMinutes()).padStart(2, '0');
+        return `${year}-${month}-${day}T${hours}:${minutes}`;
+    }
+
+    // Helper function to get current local time
+    function getCurrentLocalTime() {
+        const now = new Date();
+        // Get local time components
+        const year = now.getFullYear();
+        const month = now.getMonth();
+        const day = now.getDate();
+        const hours = now.getHours();
+        const minutes = now.getMinutes();
+        const seconds = now.getSeconds();
+        const milliseconds = now.getMilliseconds();
+        
+        // Create new date in local timezone
+        return new Date(year, month, day, hours, minutes, seconds, milliseconds);
+    }
+
     const validateForm = () => {
         const newErrors = {};
         
@@ -26,7 +55,7 @@ const TodoForm = ({ onSubmit, loading, initialData = null, isEdit = false }) =>
         // Validate deadline if set
         if (formData.deadline) {
             const selectedDate = new Date(formData.deadline);
-            const now = new Date();
+            const now = getCurrentLocalTime();
             if (selectedDate <= now) {
                 newErrors.deadline = 'Deadline must be in the future';
             }
@@ -84,9 +113,12 @@ const TodoForm = ({ onSubmit, loading, initialData = null, isEdit = false }) =>
     };
 
     const setQuickDeadline = (hours) => {
-        const now = new Date();
+        // Use local time instead of UTC
+        const now = getCurrentLocalTime();
         const deadline = new Date(now.getTime() + hours * 60 * 60 * 1000);
-        const formattedDeadline = deadline.toISOString().slice(0, 16);
+        
+        // Format for datetime-local input (preserves local timezone)
+        const formattedDeadline = formatDateForInput(deadline.toISOString());
         setFormData(prev => ({ ...prev, deadline: formattedDeadline }));
         if (errors.deadline) {
             setErrors(prev => ({ ...prev, deadline: '' }));

--- FILE: app/src/components/TodoItem.jsx ---
diff --git a/app/src/components/TodoItem.jsx b/app/src/components/TodoItem.jsx
index 46cdee1..31f0e29 100644
--- a/app/src/components/TodoItem.jsx
+++ b/app/src/components/TodoItem.jsx
@@ -7,10 +7,39 @@ const TodoItem = ({ todo, onUpdate, onToggle, onDelete, onSetDeadline, onRemoveD
         title: todo.title,
         description: todo.description || '',
         completed: todo.completed,
-        deadline: todo.deadline ? todo.deadline.slice(0, 16) : ''
+        deadline: todo.deadline ? formatDateForInput(todo.deadline) : ''
     });
     const [editErrors, setEditErrors] = useState({});
 
+    // Helper function to format date for datetime-local input (preserves local timezone)
+    function formatDateForInput(dateString) {
+        if (!dateString) return '';
+        const date = new Date(dateString);
+        // Convert to local timezone and format for datetime-local input
+        const year = date.getFullYear();
+        const month = String(date.getMonth() + 1).padStart(2, '0');
+        const day = String(date.getDate()).padStart(2, '0');
+        const hours = String(date.getHours()).padStart(2, '0');
+        const minutes = String(date.getMinutes()).padStart(2, '0');
+        return `${year}-${month}-${day}T${hours}:${minutes}`;
+    }
+
+    // Helper function to get current local time
+    function getCurrentLocalTime() {
+        const now = new Date();
+        // Get local time components
+        const year = now.getFullYear();
+        const month = now.getMonth();
+        const day = now.getDate();
+        const hours = now.getHours();
+        const minutes = now.getMinutes();
+        const seconds = now.getSeconds();
+        const milliseconds = now.getMilliseconds();
+        
+        // Create new date in local timezone
+        return new Date(year, month, day, hours, minutes, seconds, milliseconds);
+    }
+
     const validateEditForm = () => {
         const newErrors = {};
         
@@ -27,7 +56,7 @@ const TodoItem = ({ todo, onUpdate, onToggle, onDelete, onSetDeadline, onRemoveD
         // Validate deadline if set
         if (editData.deadline) {
             const selectedDate = new Date(editData.deadline);
-            const now = new Date();
+            const now = getCurrentLocalTime();
             if (selectedDate <= now) {
                 newErrors.deadline = 'Deadline must be in the future';
             }
@@ -86,7 +115,7 @@ const TodoItem = ({ todo, onUpdate, onToggle, onDelete, onSetDeadline, onRemoveD
     };
 
     const formatDate = (dateString) => {
-        return new Date(dateString).toLocaleDateString('en-US', {
+        return new Date(dateString).toLocaleDateString('ko-KR', {
             year: 'numeric',
             month: 'short',
             day: 'numeric',
@@ -97,7 +126,7 @@ const TodoItem = ({ todo, onUpdate, onToggle, onDelete, onSetDeadline, onRemoveD
 
     const formatDeadline = (deadlineString) => {
         const deadline = new Date(deadlineString);
-        const now = new Date();
+        const now = getCurrentLocalTime();
         const diffTime = deadline - now;
         const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
         const diffHours = Math.ceil(diffTime / (1000 * 60 * 60));
@@ -201,7 +230,7 @@ const TodoItem = ({ todo, onUpdate, onToggle, onDelete, onSetDeadline, onRemoveD
                                     title: todo.title, 
                                     description: todo.description || '',
                                     completed: todo.completed,
-                                    deadline: todo.deadline ? todo.deadline.slice(0, 16) : ''
+                                    deadline: todo.deadline ? formatDateForInput(todo.deadline) : ''
                                 });
                                 setEditErrors({});
                             }}

--- FILE: app/src/components/layout/Header.jsx ---
diff --git a/app/src/components/layout/Header.jsx b/app/src/components/layout/Header.jsx
index 6d0b280..4a02acf 100644
--- a/app/src/components/layout/Header.jsx
+++ b/app/src/components/layout/Header.jsx
@@ -12,6 +12,9 @@ const Header = () => {
     navigate('/login');
   };
 
+  // Extract user data from the nested structure
+  const userData = user?.data || user;
+
   return (
     <header className="header">
       <div className="header-content">
@@ -19,10 +22,10 @@ const Header = () => {
           <Link to="/todos">Todo App</Link>
         </div>
         
-        {user && (
+        {userData && (
           <nav className="nav">
             <span className="welcome-text">
-              Welcome, {user.firstName || user.username}!
+              Welcome, {userData.firstName || userData.username}!
             </span>
             <button onClick={handleLogout} className="logout-btn">
               Logout

--- FILE: app/src/context/AuthContext.jsx ---
diff --git a/app/src/context/AuthContext.jsx b/app/src/context/AuthContext.jsx
index 06c7c87..6a63af1 100644
--- a/app/src/context/AuthContext.jsx
+++ b/app/src/context/AuthContext.jsx
@@ -29,7 +29,9 @@ export const AuthProvider = ({ children }) => {
           console.log('Attempting to get current user with token');
           const userProfile = await authService.getCurrentUser();
           console.log('User profile retrieved:', userProfile);
-          setUser(userProfile);
+          // Extract user profile from nested structure if needed
+          const actualUserProfile = userProfile?.data || userProfile;
+          setUser(actualUserProfile);
         } catch (error) {
           console.error('Failed to get current user:', error);
           console.log('Clearing invalid token and logging out');
@@ -55,6 +57,9 @@ export const AuthProvider = ({ children }) => {
         refreshToken: refreshToken ? 'present' : 'null'
       });
       
+      console.log('Login response full data:', response.data);
+      console.log('User profile from login:', userProfile);
+      
       // 토큰 유효성 검사
       if (!accessToken || accessToken.trim() === '') {
         console.error('Invalid access token received');
@@ -65,7 +70,11 @@ export const AuthProvider = ({ children }) => {
       tokenStorage.setTokens(accessToken, refreshToken);
       
       setToken(accessToken);
-      setUser(userProfile);
+      // Extract user profile from nested structure if needed
+      const actualUserProfile = userProfile?.data || userProfile;
+      setUser(actualUserProfile);
+      
+      console.log('User set in context after login:', actualUserProfile);
       
       return { success: true };
     } catch (error) {
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.