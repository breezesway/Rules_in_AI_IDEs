- Rule Primary Category: Code Implementation
- Rule Secondary Category: Code Style Conventions
- Rule File Path: .kiro/steering/tech.md
- Change Type: modified
- Target Rule Content:
- - Strict TypeScript configuration
- - ESLint with naming conventions (camelCase/PascalCase imports)
- - Semicolons required
- - Curly braces enforced
- - Strict equality (===) preferred
+ - Strict TypeScript: Full type checking with ES2022 target
+ - ESLint Configuration: TypeScript-aware linting rules
+ - Naming Conventions: 
+ - camelCase for TypeScript files and variables
+ - PascalCase for React/Preact components and imports
+ - kebab-case for config files and documentation
+ - Code Quality: Semicolons required, curly braces enforced, strict equality preferred
+ - Module System: ESM with NodeNext module resolution

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: tsconfig.json, package.json, ESLint configuration files (e.g., .eslintrc.js, eslint.config.js), file system paths, and source code files (.ts, .tsx).
- Suggested Logic: 1. Inspect 'tsconfig.json' for 'compilerOptions.target' set to 'ES2022', 'compilerOptions.strict' set to true, and 'moduleResolution' set to 'NodeNext'.
2. Inspect 'package.json' for '"type": "module"'.
3. Verify file names via file system check: .ts files must be camelCase, config/docs must be kebab-case.
4. Use AST parsing on source code to enforce: PascalCase for imports/components, camelCase for variables, presence of semicolons, curly braces on blocks, and usage of '===' over '=='.
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: before_rule_commit (distance=5)
- Commit Message: Moves self-test functionality

Moves the self-test functionality from its own module into the mcp-config service.

This change consolidates related configuration and testing logic, improving code organization and maintainability.
- Changed Files List: 
src/extension.ts	+1	-2	after_lines=68
src/services/mcp-config.service.ts	+26	-0	after_lines=105
src/services/self-test.service.ts	+0	-25	after_lines=0

- Project Directory Tree (Snapshot after commit):
├── .github
│   ├── copilot-instructions.md
│   └── prompts
│       ├── AI-Knowledge-Initial.prompt.md
│       ├── AI-Knowledge-enhance-fe.prompt.md
│       ├── PRD-Draft-Generator.prompt.md
│       ├── PRD-Final-Generator.prompt.md
│       ├── Task-Executor.prompt.md
│       ├── Task-Generator.prompt.md
│       └── Test-Draft-Generator.prompt.md
├── .gitignore
├── .kiro
│   └── steering
│       ├── product.md
│       ├── structure.md
│       └── tech.md
├── .vscode
│   ├── extensions.json
│   ├── launch.json
│   ├── settings.json
│   └── tasks.json
├── .vscode-test.mjs
├── .vscodeignore
├── CHANGELOG.md
├── README.md
├── docs
│   ├── README.md
│   ├── configuration.md
│   ├── development.md
│   ├── troubleshooting.md
│   └── usage.md
├── eslint.config.mjs
├── package-lock.json
├── package.json
├── scripts
│   └── mcp-clean.mjs
├── src
│   ├── extension.ts
│   ├── mcp.server.ts
│   ├── services
│   │   ├── extension-config.service.ts
│   │   ├── mcp-config.service.ts
│   │   └── node-version.service.ts
│   ├── shared
│   │   └── constants.ts
│   └── test
│       └── extension.test.ts
└── tsconfig.json

- File Diffs (Code Changes in this Commit):
--- FILE: src/extension.ts ---
diff --git a/src/extension.ts b/src/extension.ts
index dcc6070..b96fc3a 100644
--- a/src/extension.ts
+++ b/src/extension.ts
@@ -1,7 +1,6 @@
 import * as vscode from "vscode";
 import { getEffectiveServerId, resolveNodeBin } from "./services/extension-config.service.js";
-import { upsertUserMcpConfig, maybeWriteWorkspaceConfig } from "./services/mcp-config.service.js";
-import { selfTest } from "./services/self-test.service.js";
+import { upsertUserMcpConfig, maybeWriteWorkspaceConfig, selfTest } from "./services/mcp-config.service.js";
 import { isNodeVersionSupported } from "./services/node-version.service.js";
 
 export async function activate(context: vscode.ExtensionContext) {

--- FILE: src/services/mcp-config.service.ts ---
diff --git a/src/services/mcp-config.service.ts b/src/services/mcp-config.service.ts
index 7ff674b..fca2bbd 100644
--- a/src/services/mcp-config.service.ts
+++ b/src/services/mcp-config.service.ts
@@ -2,6 +2,7 @@ import * as vscode from "vscode";
 import * as fs from "node:fs/promises";
 import * as os from "node:os";
 import * as path from "node:path";
+import { spawn } from "node:child_process";
 
 export async function upsertUserMcpConfig(nodeBin: string, serverJs: string, serverId: string): Promise<string> {
   const userDir = path.join(os.homedir(), ".kiro", "settings");
@@ -77,3 +78,28 @@ async function pathExists(p: string): Promise<boolean> {
     return false;
   }
 }
+
+
+export async function selfTest(nodeBin: string, serverJs: string): Promise<boolean> {
+  return new Promise<boolean>((resolve) => {
+    const child = spawn(nodeBin, [serverJs, "--selftest"], { stdio: ["ignore", "pipe", "pipe"] });
+    let out = "";
+    let done = false;
+
+    const finish = (ok: boolean) => {
+      if (!done) {
+        done = true;
+        resolve(ok);
+      }
+    };
+
+    child.stdout.on("data", (d) => { out += d.toString(); });
+    child.on("error", () => finish(false));
+    child.on("close", (code) => finish(code === 0 && out.includes("SELFTEST_OK")));
+
+    setTimeout(() => {
+      try { child.kill(); } catch { }
+      finish(false);
+    }, 4000);
+  });
+}
\ No newline at end of file

--- FILE: src/services/self-test.service.ts ---
diff --git a/src/services/self-test.service.ts b/src/services/self-test.service.ts
deleted file mode 100644
index 90b0184..0000000
--- a/src/services/self-test.service.ts
+++ /dev/null
@@ -1,25 +0,0 @@
-import { spawn } from "node:child_process";
-
-export async function selfTest(nodeBin: string, serverJs: string): Promise<boolean> {
-  return new Promise<boolean>((resolve) => {
-    const child = spawn(nodeBin, [serverJs, "--selftest"], { stdio: ["ignore", "pipe", "pipe"] });
-    let out = "";
-    let done = false;
-
-    const finish = (ok: boolean) => {
-      if (!done) {
-        done = true;
-        resolve(ok);
-      }
-    };
-
-    child.stdout.on("data", (d) => { out += d.toString(); });
-    child.on("error", () => finish(false));
-    child.on("close", (code) => finish(code === 0 && out.includes("SELFTEST_OK")));
-
-    setTimeout(() => {
-      try { child.kill(); } catch {}
-      finish(false);
-    }, 4000);
-  });
-}
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.