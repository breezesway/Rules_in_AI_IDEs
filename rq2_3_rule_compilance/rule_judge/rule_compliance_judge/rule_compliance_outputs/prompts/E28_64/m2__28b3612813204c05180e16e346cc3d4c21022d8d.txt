- Rule Primary Category: Architecture & Design
- Rule Secondary Category: Design References & Constraints
- Rule File Path: .kiro/steering/structure.md
- Change Type: modified
- Target Rule Content:
- - MCP Tools: ping, constellation_impactAnalysis
+ - MCP Tools: ping, constellation_impactAnalysis, constellation_onboarding.plan, constellation_onboarding.commitPlan, constellation_onboarding.nextStep, constellation_onboarding.finalize

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: Configuration files (e.g., .json, .yaml), manifest files, or source code where MCP (Model Context Protocol) tools are registered or defined.
- Suggested Logic: Search for the specific strings/identifiers added to the MCP Tools list ('constellation_onboarding.plan', 'constellation_onboarding.commitPlan', 'constellation_onboarding.nextStep', 'constellation_onboarding.finalize') within the project's tool registration manifest or definition files to ensure they are properly declared.
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=2)
- Commit Message: feat(ui): enhance button component and toolbar with new styles and functionality
- Changed Files List: 
webview-ui/src/components/Button.tsx	+7	-1	after_lines=23
webview-ui/src/components/GraphCanvas.tsx	+7	-0	after_lines=223
webview-ui/src/components/GraphDashboard.tsx	+17	-4	after_lines=251
webview-ui/src/components/GraphToolbar.tsx	+15	-11	after_lines=118
webview-ui/src/services/graph-styles.service.ts	+12	-6	after_lines=203
webview-ui/src/styles/global.css	+122	-15	after_lines=881

- Project Directory Tree (Snapshot after commit):
├── .github
│   ├── copilot-instructions.md
│   └── prompts
│       ├── AI-Knowledge-Initial.prompt.md
│       ├── AI-Knowledge-enhance-fe.prompt.md
│       ├── PRD-Draft-Generator.prompt.md
│       ├── PRD-Final-Generator.prompt.md
│       ├── Task-Executor.prompt.md
│       ├── Task-Generator.prompt.md
│       └── Test-Draft-Generator.prompt.md
├── .gitignore
├── .kiro
│   ├── specs
│   │   ├── 01-dependency-cruiser-integration
│   │   │   ├── depency-cruiser-implmentation-prd.md
│   │   │   ├── design.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 02-cytoscape-graph-visualization
│   │   │   ├── cytoscape-graph-initial-prd.md
│   │   │   ├── design.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 03-graph-view-layout
│   │   │   ├── design.md
│   │   │   ├── graph-view-layout-prd.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 04-impact-analysis-phase-1
│   │   │   ├── design.md
│   │   │   ├── impact-analysis-phase-1-prd.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 05-onboarding-agent-phase-1
│   │   │   ├── design.md
│   │   │   ├── onboard-agent-phase-1-prd.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 06-onboarding-agent-phase-2
│   │   │   ├── design.md
│   │   │   ├── onboarding-agent-phase-2.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   ├── 07-onboarding-agent-phase-3
│   │   │   ├── design.md
│   │   │   ├── onboarding-agent-phase-3-prd.md
│   │   │   ├── requirements.md
│   │   │   └── tasks.md
│   │   └── 08-steering-docs-update
│   │       ├── design.md
│   │       ├── requirements.md
│   │       └── tasks.md
│   └── steering
│       ├── dependency-scanning.md
│       ├── graph-visualization.md
│       ├── impact-analysis.md
│       ├── messaging.md
│       ├── onboarding.md
│       ├── product.md
│       ├── structure.md
│       └── tech.md
├── .vscode
│   ├── extensions.json
│   ├── launch.json
│   ├── settings.json
│   └── tasks.json
├── .vscode-test.mjs
├── .vscodeignore
├── README.md
├── docs
│   ├── README.md
│   ├── configuration.md
│   ├── development.md
│   ├── events.md
│   ├── troubleshooting.md
│   └── usage.md
├── eslint.config.mjs
├── media
│   └── kiro.svg
├── package-lock.json
├── package.json
├── scripts
│   └── mcp-clean.mjs
├── src
│   ├── extension.ts
│   ├── mcp.server.ts
│   ├── personas
│   │   └── onboarding.md
│   ├── providers
│   │   └── graph-panel-provider.ts
│   ├── services
│   │   ├── dependency-cruiser.service.ts
│   │   ├── extension-config.service.ts
│   │   ├── graph-data.service.ts
│   │   ├── http-bridge.service.ts
│   │   ├── impact-analysis.service.ts
│   │   ├── mcp-config.service.ts
│   │   ├── message-router.service.ts
│   │   ├── messenger.service.ts
│   │   ├── node-version.service.ts
│   │   ├── onboarding-mode.service.ts
│   │   ├── onboarding-walkthrough.service.ts
│   │   ├── security.service.ts
│   │   ├── webview.service.ts
│   │   └── workspace.service.ts
│   ├── shared
│   │   ├── constants.ts
│   │   └── graph.types.ts
│   ├── side-panel-view-provider.ts
│   └── test
│       └── extension.test.ts
├── tsconfig.json
├── ui-ux-phase-1.prd.md
└── webview-ui
    ├── index.html
    ├── src
    │   ├── components
    │   │   ├── App.tsx
    │   │   ├── Button.tsx
    │   │   ├── GraphCanvas.tsx
    │   │   ├── GraphDashboard.tsx
    │   │   ├── GraphToolbar.tsx
    │   │   ├── OnboardingModeToggle.tsx
    │   │   └── OnboardingStatus.tsx
    │   ├── main.tsx
    │   ├── services
    │   │   ├── extension-config.service.ts
    │   │   ├── file-type.service.ts
    │   │   ├── graph-styles.service.ts
    │   │   └── messenger.ts
    │   ├── styles
    │   │   └── global.css
    │   ├── types
    │   │   └── vscode-webview.d.ts
    │   └── views
    │       └── SidePanelView.tsx
    ├── tsconfig.json
    └── vite.config.ts

- File Diffs (Code Changes in this Commit):
--- FILE: webview-ui/src/components/Button.tsx ---
diff --git a/webview-ui/src/components/Button.tsx b/webview-ui/src/components/Button.tsx
index 1304a76..99db839 100644
--- a/webview-ui/src/components/Button.tsx
+++ b/webview-ui/src/components/Button.tsx
@@ -8,8 +8,14 @@ interface ButtonProps extends Omit<JSX.HTMLAttributes<HTMLButtonElement>, 'type'
 }
 
 export function Button({ children, onClick, type = 'button', ...rest }: ButtonProps) {
+  // Allow callers to provide `class` (Preact) or `className` (JSX typing) variants.
+  // We merge with the base .button to preserve existing styling.
+  const cls = ((rest as any).class ?? (rest as any).className ?? '').toString().trim()
+  const merged = ['button', 'btn', cls].filter(Boolean).join(' ')
+  const props = { ...rest } as any
+  delete props.className
   return (
-    <button class="button" type={type} onClick={onClick} {...rest}>
+    <button class={merged} type={type} onClick={onClick} {...props}>
       {children}
     </button>
   )

--- FILE: webview-ui/src/components/GraphCanvas.tsx ---
diff --git a/webview-ui/src/components/GraphCanvas.tsx b/webview-ui/src/components/GraphCanvas.tsx
index afcb907..4540cb9 100644
--- a/webview-ui/src/components/GraphCanvas.tsx
+++ b/webview-ui/src/components/GraphCanvas.tsx
@@ -147,6 +147,13 @@ export function GraphCanvas({ data, isRendering, onRenderingChange, impactSource
             layout: layoutConfig
           })
 
+          // Inject a synthetic data property on nodes so the label outline can reference the webview background
+          // We re-use Cytoscape's data API to expose a runtime-derived color without re-creating the stylesheet
+          const bg = getComputedStyle(document.documentElement).getPropertyValue('--vscode-editor-background').trim() || '#1e1e1e'
+          cyRef.current.nodes().forEach(n => {
+            n.data('textOutlineColor', bg)
+          })
+
           // Add event handlers for interactions
           cyRef.current.on('tap', 'node', (evt) => {
             // Single-click highlighting (node gets selected automatically by Cytoscape)

--- FILE: webview-ui/src/components/GraphDashboard.tsx ---
diff --git a/webview-ui/src/components/GraphDashboard.tsx b/webview-ui/src/components/GraphDashboard.tsx
index 2eba763..64bf101 100644
--- a/webview-ui/src/components/GraphDashboard.tsx
+++ b/webview-ui/src/components/GraphDashboard.tsx
@@ -2,6 +2,7 @@ import { useCallback, useEffect, useState, useRef } from 'preact/hooks'
 import { messenger } from '../services/messenger'
 import { GraphToolbar } from './GraphToolbar'
 import { GraphCanvas } from './GraphCanvas'
+import { Button } from './Button'
 
 interface Node {
   id: string
@@ -189,30 +190,42 @@ export function GraphDashboard() {
       
       {/* Main Content Area */}
       <div className="graph-dashboard-content">
+        {/* Impact banner */}
+        {impactState.isActive && (
+          <div className="banner-impact" role="status" aria-live="polite">
+            <span className="banner-text">Impact View — source: {impactState.data?.sourceFile}</span>
+            <Button class="btn-secondary btn-sm" onClick={handleResetImpactView} data-testid="graph-reset-banner-button">↺ Reset View</Button>
+          </div>
+        )}
+
         {/* Loading State */}
         {state.type === 'loading' && (
-          <div className="graph-dashboard-message">
+          <div className="state-message">
+            <div>⌛</div>
             <p>Loading graph...</p>
           </div>
         )}
         
         {/* Scanning State */}
         {state.type === 'scanning' && (
-          <div className="graph-dashboard-message">
+          <div className="state-message">
+            <div>🔎</div>
             <p>{state.message}</p>
           </div>
         )}
         
         {/* Rendering State */}
         {state.type === 'rendering' && (
-          <div className="graph-dashboard-message">
+          <div className="state-message">
+            <div>🧩</div>
             <p>{state.message}</p>
           </div>
         )}
         
         {/* Error State */}
         {state.type === 'error' && (
-          <div className="graph-dashboard-message graph-dashboard-error">
+          <div className="state-message graph-dashboard-error">
+            <div>⚠</div>
             <p>Error: {state.message}</p>
             <button 
               onClick={() => messenger.post('graph/load')}

--- FILE: webview-ui/src/components/GraphToolbar.tsx ---
diff --git a/webview-ui/src/components/GraphToolbar.tsx b/webview-ui/src/components/GraphToolbar.tsx
index 5b0f2f9..eabb9ec 100644
--- a/webview-ui/src/components/GraphToolbar.tsx
+++ b/webview-ui/src/components/GraphToolbar.tsx
@@ -24,6 +24,7 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
 
   return (
     <div className="toolbar">
+      {/* Row 1: search + actions */}
       <div className="toolbar-row">
         {/* Search Input - Placeholder only */}
         <input
@@ -35,7 +36,7 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
           {...getPlaceholderAttributes(isToolbarFeatureEnabled('searchEnabled'))}
           data-testid="graph-search-input"
         />
-        
+
         {/* Action Buttons */}
         <div className="toolbar-actions">
           <button
@@ -45,19 +46,21 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
             {...getPlaceholderAttributes(isToolbarFeatureEnabled('fitEnabled'))}
             data-testid="graph-fit-button"
           >
-            Fit
+            {/* simple inline icon */}
+            <span aria-hidden>⤢</span> Fit
           </button>
-          
+
           {/* Reset View Button - Only show when impact filter is active */}
           {impactState?.isActive && onResetImpactView && (
             <Button 
               onClick={onResetImpactView}
               data-testid="graph-reset-view-button"
+              class="btn-secondary btn-sm"
             >
-              Reset View
+              ↺ Reset
             </Button>
           )}
-          
+
           <div className="toolbar-dropdown">
             <button
               className="toolbar-button toolbar-dropdown-button"
@@ -66,17 +69,18 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
               {...getPlaceholderAttributes(isToolbarFeatureEnabled('layoutEnabled'))}
               data-testid="graph-layout-button"
             >
-              Layout ▼
+              ⚙ Layout ▼
             </button>
           </div>
-          
+
           {/* Re-scan Button - Functional */}
-          <Button onClick={onRescan} data-testid="graph-rescan-button">
-            Re-scan
+          <Button onClick={onRescan} data-testid="graph-rescan-button" class="btn-primary btn-sm">
+            ⟳ Re-scan
           </Button>
         </div>
       </div>
       
+      {/* Row 2: filters + stats */}
       <div className="toolbar-row">
         {/* Filter Chips - Placeholders */}
         <div className="toolbar-filters">
@@ -99,7 +103,7 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
         {(nodeCount !== undefined && edgeCount !== undefined) && (
           <div className="toolbar-stats">
             <span className="toolbar-stat">
-              {nodeCount} nodes, {edgeCount} edges
+              {nodeCount} nodes • {edgeCount} edges
             </span>
             {isOptimized && (
               <span className="toolbar-stat toolbar-stat-optimized">
@@ -111,4 +115,4 @@ export function GraphToolbar({ onRescan, nodeCount, edgeCount, isOptimized, impa
       </div>
     </div>
   )
-}
\ No newline at end of file
+}

--- FILE: webview-ui/src/services/graph-styles.service.ts ---
diff --git a/webview-ui/src/services/graph-styles.service.ts b/webview-ui/src/services/graph-styles.service.ts
index 6f02cdc..b9c98bc 100644
--- a/webview-ui/src/services/graph-styles.service.ts
+++ b/webview-ui/src/services/graph-styles.service.ts
@@ -83,6 +83,8 @@ export function generateGraphStylesheet(nodeCount: number = 0) {
         'font-size': isLargeGraph ? '10px' : '12px',
         'text-valign': 'center',
         'text-halign': 'center',
+        'text-outline-width': 1,
+        'text-outline-color': 'data(textOutlineColor)',
         'width': isLargeGraph ? '20px' : '30px',
         'height': isLargeGraph ? '20px' : '30px',
         'background-color': colors.other // Default fallback
@@ -92,12 +94,12 @@ export function generateGraphStylesheet(nodeCount: number = 0) {
     {
       selector: 'edge',
       style: {
-        'line-color': '#666666',
+        'line-color': '#777777',
         'width': isLargeGraph ? 1 : 2,
-        'opacity': isLargeGraph ? 0.4 : 0.6,
+        'opacity': isLargeGraph ? 0.35 : 0.6,
         'curve-style': 'bezier',
         'target-arrow-shape': 'triangle',
-        'target-arrow-color': '#666666',
+        'target-arrow-color': '#777777',
         'arrow-scale': 1.0
       }
     },
@@ -107,8 +109,8 @@ export function generateGraphStylesheet(nodeCount: number = 0) {
       style: {
         'line-color': '#FF8C00',
         'target-arrow-color': '#FF8C00',
-        'width': isLargeGraph ? 2 : 3,
-        'opacity': 0.9
+        'width': isLargeGraph ? 2.5 : 3.5,
+        'opacity': 0.95
       }
     },
     // Selected node style
@@ -117,7 +119,11 @@ export function generateGraphStylesheet(nodeCount: number = 0) {
       style: {
         'border-width': 3,
         'border-color': '#007FD4',
-        'background-color': '#FF8C00'
+        'shadow-blur': 18,
+        'shadow-color': '#007FD4',
+        'shadow-opacity': 0.5,
+        'shadow-offset-x': 0,
+        'shadow-offset-y': 0
       }
     },
     // Epicenter node style (impact source)

--- FILE: webview-ui/src/styles/global.css ---
diff --git a/webview-ui/src/styles/global.css b/webview-ui/src/styles/global.css
index 6af597e..3ef1572 100644
--- a/webview-ui/src/styles/global.css
+++ b/webview-ui/src/styles/global.css
@@ -2,6 +2,44 @@
 :root {
   color-scheme: light dark;
   
+  /* Spacing tokens */
+  --kiro-space-1: 4px;
+  --kiro-space-2: 8px;
+  --kiro-space-3: 12px;
+  --kiro-space-4: 16px;
+  --kiro-space-6: 24px;
+  --kiro-space-8: 32px;
+
+  /* Radius tokens */
+  --kiro-radius-1: 4px;
+  --kiro-radius-2: 8px;
+  --kiro-radius-pill: 9999px;
+
+  /* Shadow tokens */
+  --kiro-shadow-1: 0 1px 2px var(--vscode-widget-shadow, rgba(0,0,0,0.25));
+  --kiro-shadow-soft: 0 6px 16px rgba(0,0,0,0.2);
+
+  /* Surface tokens */
+  --kiro-surface-0: var(--vscode-editor-background);
+  --kiro-surface-1: var(--vscode-editorWidget-background);
+  --kiro-surface-2: var(--vscode-sideBar-background);
+  --kiro-border: var(--vscode-widget-border, rgba(255,255,255,0.08));
+
+  /* Text tokens */
+  --kiro-text: var(--vscode-foreground);
+  --kiro-text-muted: var(--vscode-descriptionForeground);
+
+  /* Accents & status */
+  --kiro-accent: var(--vscode-charts-blue, #007ACC);
+  --kiro-accent-contrast: var(--vscode-button-foreground, #fff);
+  --kiro-success: var(--vscode-testing-iconPassed, #2ea043);
+  --kiro-warning: var(--vscode-list-warningForeground, #ffcc00);
+  --kiro-danger: var(--vscode-errorForeground, #f48771);
+  --kiro-info: var(--vscode-notificationsInfoIcon-foreground, #4FC1FF);
+
+  /* Graph grid */
+  --kiro-grid-color: color-mix(in srgb, var(--vscode-foreground) 8%, transparent);
+  
   /* File type node colors - can be overridden by themes */
   --kiro-node-ts: #569CD6;
   --kiro-node-tsx: #4FC1FF;
@@ -32,10 +70,46 @@ body {
   background: var(--vscode-button-background);
   color: var(--vscode-button-foreground);
   border: 1px solid var(--vscode-button-border, transparent);
-  border-radius: 4px;
+  border-radius: var(--kiro-radius-1);
   cursor: pointer;
 }
 
+/* New button system (variants layered on top of existing .button for back-compat) */
+.btn { 
+  display: inline-flex; 
+  align-items: center; 
+  gap: 6px; 
+  padding: 6px 10px; 
+  font-size: 12px; 
+  border-radius: var(--kiro-radius-1); 
+  border: 1px solid var(--vscode-button-border, transparent); 
+  cursor: pointer; 
+}
+.btn-primary { 
+  background: var(--vscode-button-background); 
+  color: var(--vscode-button-foreground); 
+}
+.btn-primary:hover:not(:disabled) { 
+  background: var(--vscode-button-hoverBackground); 
+}
+.btn-secondary { 
+  background: var(--vscode-button-secondaryBackground, var(--vscode-button-background)); 
+  color: var(--vscode-button-secondaryForeground, var(--vscode-button-foreground)); 
+}
+.btn-secondary:hover:not(:disabled) { 
+  background: var(--vscode-button-secondaryHoverBackground, var(--vscode-button-hoverBackground)); 
+}
+.btn-ghost { 
+  background: transparent; 
+  color: var(--kiro-text); 
+  border-color: var(--kiro-border); 
+}
+.btn-ghost:hover:not(:disabled) { 
+  background: color-mix(in srgb, var(--vscode-foreground) 10%, transparent); 
+}
+.btn-sm { padding: 4px 8px; font-size: 11px; }
+.btn-md { padding: 6px 10px; font-size: 12px; }
+
 .button:hover {
   background: var(--vscode-button-hoverBackground);
 }
@@ -47,6 +121,19 @@ body {
   flex-direction: column;
 }
 
+/* Unified state message utility */
+.state-message {
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  transform: translate(-50%, -50%);
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: var(--kiro-space-2);
+  color: var(--kiro-text);
+}
+
 .graph-dashboard-content {
   flex: 1;
   position: relative;
@@ -85,17 +172,17 @@ body {
 .toolbar {
   display: flex;
   flex-direction: column;
-  gap: 8px;
-  padding: 12px;
-  background: var(--vscode-editorWidget-background);
-  border-bottom: 1px solid var(--vscode-widget-border);
+  gap: var(--kiro-space-2);
+  padding: var(--kiro-space-3);
+  background: var(--kiro-surface-1);
+  border-bottom: 1px solid var(--kiro-border);
   min-height: 40px;
 }
 
 .toolbar-row {
   display: flex;
   align-items: center;
-  gap: 8px;
+  gap: var(--kiro-space-2);
   flex-wrap: wrap;
 }
 
@@ -107,7 +194,7 @@ body {
   background: var(--vscode-input-background);
   color: var(--vscode-input-foreground);
   border: 1px solid var(--vscode-input-border, transparent);
-  border-radius: 4px;
+  border-radius: var(--kiro-radius-1);
   outline: none;
 }
 
@@ -123,7 +210,7 @@ body {
 .toolbar-actions {
   display: flex;
   align-items: center;
-  gap: 8px;
+  gap: var(--kiro-space-2);
 }
 
 .toolbar-button {
@@ -132,7 +219,7 @@ body {
   background: var(--vscode-button-secondaryBackground, var(--vscode-button-background));
   color: var(--vscode-button-secondaryForeground, var(--vscode-button-foreground));
   border: 1px solid var(--vscode-button-border, transparent);
-  border-radius: 4px;
+  border-radius: var(--kiro-radius-1);
   cursor: pointer;
   white-space: nowrap;
 }
@@ -165,7 +252,7 @@ body {
 
 .toolbar-label {
   font-size: 12px;
-  color: var(--vscode-descriptionForeground);
+  color: var(--kiro-text-muted);
   font-weight: 500;
 }
 
@@ -193,7 +280,7 @@ body {
   margin-left: auto;
   display: flex;
   align-items: center;
-  gap: 8px;
+  gap: var(--kiro-space-2);
 }
 
 .toolbar-stat {
@@ -212,9 +299,28 @@ body {
   position: relative;
 }
 
+/* Impact banner */
+.banner-impact {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--kiro-space-2);
+  padding: 8px 12px;
+  background: color-mix(in srgb, var(--kiro-accent) 15%, transparent);
+  border-bottom: 1px solid var(--kiro-border);
+}
+.banner-impact .banner-text {
+  font-size: 12px;
+  color: var(--kiro-text);
+}
+
 .graph-canvas-container {
   width: 100%;
   height: 100%;
+  background-image: 
+    linear-gradient(to right, var(--kiro-grid-color) 1px, transparent 1px),
+    linear-gradient(to bottom, var(--kiro-grid-color) 1px, transparent 1px);
+  background-size: 24px 24px;
 }
 
 .graph-canvas-overlay {
@@ -241,7 +347,8 @@ body {
 /* Focus and Keyboard Navigation */
 .toolbar-input:focus,
 .toolbar-button:focus,
-.toolbar-chip:focus {
+.toolbar-chip:focus,
+.btn:focus {
   outline: 2px solid var(--vscode-focusBorder);
   outline-offset: 2px;
 }
@@ -297,9 +404,9 @@ body {
 
 /* Onboarding Mode Toggle Styles */
 .onboarding-mode-toggle {
-  padding: 12px;
-  border-bottom: 1px solid var(--vscode-widget-border);
-  background: var(--vscode-editorWidget-background);
+  padding: var(--kiro-space-3);
+  border-bottom: 1px solid var(--kiro-border);
+  background: var(--kiro-surface-1);
 }
 
 .mode-toggle-header {
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.