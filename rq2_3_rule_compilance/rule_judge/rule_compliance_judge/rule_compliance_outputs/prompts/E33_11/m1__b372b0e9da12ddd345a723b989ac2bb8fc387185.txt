- Rule Primary Category: Code Implementation
- Rule Secondary Category: Code Style Conventions
- Rule File Path: internal/domain/.cursor/rules/entity.mdc
- Change Type: added
- Target Rule Content:
All fields are generated with JSON tags in snake_case by default. Fields marked with the 'secret' tag are not marshaled to JSON.

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: Source code files containing data structures with field tags or decorators (e.g., Go structs, Rust structs with serde, or TypeScript classes with decorators).
- Suggested Logic: 1. Scan source code for struct/class field definitions.
2. Locate 'json' tags/annotations and use a regular expression to verify that the tag value follows snake_case (e.g., `^[a-z0-9_]+$`).
3. Check for the presence of a 'secret' tag/annotation on fields.
4. If a 'secret' tag is present, verify that the 'json' tag is set to omit the field (e.g., `json:"-"` in Go).
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=1)
- Commit Message: docs: update tenant entity rule for meta-driven code generation

- Define Tenant entity fields with types, constructor inputs, defaults, and secret tags
- Align field definitions with entity.mdc meta-rules for consistent code generation
- Add note referencing entity.mdc for code generation, validation, and business logic guidance
- Improve clarity and maintainability of tenant entity documentation
- Changed Files List: 
internal/domain/tenant/.cursor/rules/tenant.mdc	+23	-0	after_lines=23
internal/domain/tenant/tenant.go	+55	-0	after_lines=55

- Project Directory Tree (Snapshot after commit):
├── .cursor
│   └── rules
│       └── global.mdc
├── README.md
├── docs
│   ├── .cursor
│   │   └── rules
│   │       ├── architecture.mdc
│   │       ├── datamodel.mdc
│   │       ├── tenant.mdc
│   │       └── user-journey.mdc
│   ├── architecture.md
│   ├── data-model.md
│   ├── tenant.md
│   └── user-journey.md
├── go.mod
└── internal
    └── domain
        ├── .cursor
        │   └── rules
        │       ├── entity.mdc
        │       └── generic-types.mdc
        └── tenant
            ├── .cursor
            │   └── rules
            │       └── tenant.mdc
            └── tenant.go

- File Diffs (Code Changes in this Commit):
--- FILE: internal/domain/tenant/.cursor/rules/tenant.mdc ---
diff --git a/internal/domain/tenant/.cursor/rules/tenant.mdc b/internal/domain/tenant/.cursor/rules/tenant.mdc
new file mode 100644
index 0000000..7348404
--- /dev/null
+++ b/internal/domain/tenant/.cursor/rules/tenant.mdc
@@ -0,0 +1,23 @@
+# Tenant
+
+This rules describes about **tenant** entity and it's behaviour
+
+---
+
+## Entity
+
+### Tenant
+
+- ID: Identity of Tenant - New Identity
+- Name: string - constructor input
+- Status: Enumerated string of "active", "inactive", default("active")
+- CreatedAt: time.Time - Default: now
+- UpdatedAt: time.Time - Default: now
+- DeletedAt: time.Time - Default: nil, secret
+- IsDeleted: boolean - Default : false, secret
+
+---
+
+Note: This entity definition follows the meta-rules in `entity.mdc` for code generation. For validation rules (e.g., required fields) or business logic (e.g., allowed status transitions), add a section below or reference a related rule file.
+
+

--- FILE: internal/domain/tenant/tenant.go ---
diff --git a/internal/domain/tenant/tenant.go b/internal/domain/tenant/tenant.go
new file mode 100644
index 0000000..a875eff
--- /dev/null
+++ b/internal/domain/tenant/tenant.go
@@ -0,0 +1,55 @@
+package tenant
+
+import (
+    "time"
+    "github.com/google/uuid"
+)
+
+// TenantID represents the unique identity of a tenant.
+type TenantID uuid.UUID
+
+// TenantStatus represents the status of a tenant.
+type TenantStatus string
+
+const (
+    // TenantStatusActive indicates the tenant is active.
+    TenantStatusActive TenantStatus = "active"
+    // TenantStatusInactive indicates the tenant is inactive.
+    TenantStatusInactive TenantStatus = "inactive"
+    // TenantStatusDefault is the default status for a tenant.
+    TenantStatusDefault TenantStatus = TenantStatusActive
+)
+
+// Tenant represents a logical customer or organization using the application.
+type Tenant struct {
+    ID          TenantID      `json:"id"`           // Unique identifier for the tenant
+    Name        string        `json:"name"`         // Tenant name
+    Status      TenantStatus  `json:"status"`       // Status of the tenant (active or inactive)
+    CreatedAt   time.Time     `json:"created_at"`   // Timestamp of tenant creation
+    UpdatedAt   time.Time     `json:"updated_at"`   // Timestamp of last update
+    DeletedAt   *time.Time    `json:"-"`            // Timestamp of deletion (if soft deleted), not marshaled
+    IsDeleted   bool          `json:"-"`            // Soft delete flag, not marshaled
+}
+
+// NewTenant creates a new Tenant with the given name and status.
+// If status is empty, it defaults to TenantStatusActive.
+// Returns an error if ID generation fails.
+func NewTenant(name string, status TenantStatus) (*Tenant, error) {
+    id, err := uuid.New()
+    if err != nil {
+        return nil, err
+    }
+    now := time.Now().UTC()
+    if status == "" {
+        status = TenantStatusDefault
+    }
+    return &Tenant{
+        ID:        TenantID(id),
+        Name:      name,
+        Status:    status,
+        CreatedAt: now,
+        UpdatedAt: now,
+        DeletedAt: nil,
+        IsDeleted: false,
+    }, nil
+} 
\ No newline at end of file
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.