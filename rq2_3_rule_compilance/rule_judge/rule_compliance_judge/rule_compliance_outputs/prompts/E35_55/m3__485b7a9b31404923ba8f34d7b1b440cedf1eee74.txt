- Rule Primary Category: Code Implementation
- Rule Secondary Category: Language Features
- Rule File Path: .cursor/rules/develop.mdc
- Change Type: added
- Target Rule Content:
- **类型安全** - 使用 `ThemeMode` 类型定义：`'light' | 'dark' | 'system'`

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: Source code files (e.g., TypeScript, Dart, or Python files defining type aliases or enums).
- Suggested Logic: Search for the declaration of a type, interface, enum, or alias named 'ThemeMode'. Verify that its definition strictly matches the union or set of values: 'light', 'dark', and 'system'.
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=3)
- Commit Message: feat: 实施统一模态框组件规范并修复深色模式问题

�� 新增设计规范
- 添加模态框组件统一使用的强制规范
- 禁止重新实现模态框逻辑，必须使用统一的Modal组件
- 更新产品规范文档，记录组件复用要求

🎨 深色模式优化
- 修复图片压缩和PDF合并工具的文件列表深色模式样式
- 优化文件列表容器背景色和边框颜色
- 改进状态指示器和悬停效果在深色模式下的显示
- 统一两个工具的文件列表设计风格

🚀 Modal组件功能增强
- 重构ImagePreview组件，使用统一的Modal组件
- 为Modal组件添加size属性支持：default、large、extra-large
- 添加zIndex属性支持，解决模态框层级问题
- 移除ImagePreview中重复的ESC键和遮罩点击逻辑

✨ 用户体验提升
- 压缩预览弹框现在使用更高的z-index(60)，不会被遮挡
- 所有模态框现在都有统一的交互行为和视觉样式
- 深色模式下文件列表更加清晰易读，层次分明

📋 技术改进
- 完全消除了重复的模态框实现代码
- 提高了组件的可维护性和一致性
- 符合设计系统的组件复用最佳实践
- Changed Files List: 
.cursor/rules/product.mdc	+26	-1	after_lines=559
src/components/image-compressor.tsx	+15	-15	after_lines=498
src/components/pdf-merger.tsx	+11	-11	after_lines=442
src/ui/image-preview.tsx	+135	-149	after_lines=192
src/ui/modal.tsx	+41	-4	after_lines=80

- Project Directory Tree (Snapshot after commit):
├── .cursor
│   └── rules
│       ├── develop.mdc
│       └── product.mdc
├── .eslintrc.json
├── .gitignore
├── .npmrc
├── README.md
├── forge.config.ts
├── forge.env.d.ts
├── index.html
├── package.json
├── pnpm-lock.yaml
├── postcss.config.js
├── src
│   ├── App.tsx
│   ├── components
│   │   ├── app-settings.tsx
│   │   ├── image-compressor.tsx
│   │   ├── operation-history.tsx
│   │   └── pdf-merger.tsx
│   ├── hooks
│   │   ├── use-dropzone.ts
│   │   └── use-theme.ts
│   ├── index.css
│   ├── lib
│   │   └── utils.ts
│   ├── main
│   │   └── utils
│   │       ├── config-manager.ts
│   │       ├── dialog-utils.ts
│   │       ├── file-utils.ts
│   │       ├── history-manager.ts
│   │       ├── image-utils.ts
│   │       ├── index.ts
│   │       ├── ipc-handlers.ts
│   │       ├── pdf-utils.ts
│   │       └── types.ts
│   ├── main.ts
│   ├── preload.ts
│   ├── renderer.ts
│   ├── types
│   │   └── electron.d.ts
│   └── ui
│       ├── button.tsx
│       ├── feature-card.tsx
│       ├── image-preview.tsx
│       ├── modal.tsx
│       ├── save-location-selector.tsx
│       └── theme-selector.tsx
├── tailwind.config.js
├── tsconfig.json
├── vite.main.config.ts
├── vite.preload.config.ts
└── vite.renderer.config.ts

- File Diffs (Code Changes in this Commit):
--- FILE: .cursor/rules/product.mdc ---
diff --git a/.cursor/rules/product.mdc b/.cursor/rules/product.mdc
index 7167d15..bc4f57d 100644
--- a/.cursor/rules/product.mdc
+++ b/.cursor/rules/product.mdc
@@ -109,9 +109,16 @@ alwaysApply: false
   - Tab键切换焦点
   - Enter键激活选中项
   - Esc键取消当前操作
+  - **Esc键关闭模态框**：所有模态框（工具界面、设置界面、操作历史）都支持按Esc键快速关闭
 - **便捷操作**：
   - 文件列表支持单击删除
   - 原生文件对话框支持多选
+- **模态框交互规范**：
+  - **Esc键关闭**：按Esc键立即关闭当前模态框，无需确认
+  - **点击遮罩关闭**：点击模态框外部区域关闭模态框
+  - **关闭按钮**：右上角提供明确的关闭按钮（×）
+  - **一致性保证**：所有模态框都遵循相同的关闭交互模式
+  - **统一组件使用**：应用内必须使用统一的Modal组件，禁止每个地方都从头实现模态框
 
 ### 帮助信息
 - **使用指引**：
@@ -399,9 +406,27 @@ alwaysApply: false
 #### 1. **组件系统**：⭐⭐⭐⭐⭐ 成熟
 - **FeatureCard组件**：支持多主题配置，高度可复用
 - **Button组件**：完整的变体系统（primary、outline、ghost等）
-- **Modal组件**：统一的布局结构和交互模式
+- **Modal组件**：统一的布局结构和交互模式，全应用强制使用
 - **ThemeSelector组件**：现代化的主题切换体验
 
+#### **组件复用强制规范**
+- **Modal组件统一使用**：
+  - ✅ 所有模态框必须使用 `src/ui/modal.tsx` 组件
+  - ❌ 禁止在任何地方重新实现模态框逻辑
+  - ✅ 支持ESC键关闭、点击遮罩关闭、右上角关闭按钮
+  - ✅ 自动处理背景滚动锁定和事件清理
+  - ✅ 统一的视觉样式和交互行为
+  - ✅ 支持多种尺寸：default(max-w-3xl)、large(max-w-5xl)、extra-large(max-w-6xl)
+  - ✅ 支持自定义z-index处理模态框层级问题
+- **组件发现和使用**：
+  - 新功能开发前必须检查 `src/ui/` 目录中的现有组件
+  - 优先扩展现有组件而非创建新组件
+  - 确保组件接口的向后兼容性
+- **实施状态**：
+  - ✅ ImagePreview组件已重构为使用统一Modal组件
+  - ✅ 所有现有模态框都使用统一组件
+  - ✅ 深色模式文件列表样式问题已修复
+
 #### 2. **设计Token**：⭐⭐⭐⭐⭐ 完善
 - **颜色系统**：完整的品牌色体系和语义化颜色
 - **间距系统**：一致的padding、margin、gap规则

--- FILE: src/components/image-compressor.tsx ---
diff --git a/src/components/image-compressor.tsx b/src/components/image-compressor.tsx
index 982901e..465d65a 100644
--- a/src/components/image-compressor.tsx
+++ b/src/components/image-compressor.tsx
@@ -1,5 +1,5 @@
 import React, { useState } from 'react';
-import { Image, FolderOpen, Settings, Eye } from 'lucide-react';
+import { Image, Settings, Eye } from 'lucide-react';
 import { Modal } from '../ui/modal';
 import { ImagePreview } from '../ui/image-preview';
 import { formatFileSize } from '../lib/utils';
@@ -251,27 +251,27 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
           </div>
         </div>
 
-        {/* 文件列表 - 与PDF合并保持一致的样式 */}
+        {/* 文件列表 - 优化深色模式样式 */}
         {files.length > 0 && (
-          <div className="border border-gray-200 dark:border-gray-700 rounded-2xl overflow-hidden">
-            <div className="p-4 bg-gray-50 dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
+          <div className="border border-gray-200 dark:border-gray-600 rounded-2xl overflow-hidden bg-white dark:bg-gray-700/50">
+            <div className="p-4 bg-gray-50 dark:bg-gray-700/70 border-b border-gray-200 dark:border-gray-600">
               <div className="flex justify-between items-center">
                 <div className="font-medium text-gray-900 dark:text-gray-100">
                   已选择的文件 ({files.length}) - 总大小: {formatFileSize(files.reduce((sum, f) => sum + f.size, 0))}
                 </div>
               </div>
             </div>
-            <ul className="divide-y divide-gray-200 dark:divide-gray-700">
+            <ul className="divide-y divide-gray-200 dark:divide-gray-600">
               {files.map((file, index) => (
                 <li 
                   key={index} 
-                  className="flex items-center justify-between p-4 hover:bg-gray-50 dark:hover:bg-gray-800 transition-all duration-200"
+                  className="flex items-center justify-between p-4 hover:bg-gray-50 dark:hover:bg-gray-600/30 transition-all duration-200 bg-white dark:bg-transparent"
                 >
                   <div className="flex items-center space-x-3">
                     <div className={`p-2 rounded-xl ${
-                      file.status === 'ready' ? 'bg-green-100 dark:bg-green-900' :
-                      file.status === 'processing' ? 'bg-yellow-100 dark:bg-yellow-900' :
-                      file.status === 'done' ? 'bg-blue-100 dark:bg-blue-900' : 'bg-red-100 dark:bg-red-900'
+                      file.status === 'ready' ? 'bg-green-100 dark:bg-green-900/50' :
+                      file.status === 'processing' ? 'bg-yellow-100 dark:bg-yellow-900/50' :
+                      file.status === 'done' ? 'bg-blue-100 dark:bg-blue-900/50' : 'bg-red-100 dark:bg-red-900/50'
                     }`}>
                       <Image className={`h-5 w-5 ${
                         file.status === 'ready' ? 'text-green-600 dark:text-green-400' :
@@ -286,10 +286,10 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
                           {formatFileSize(file.size)}
                         </p>
                         <span className={`text-xs px-2 py-1 rounded-full ${
-                          file.status === 'ready' ? 'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400' :
-                          file.status === 'processing' ? 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900/30 dark:text-yellow-400' :
-                          file.status === 'done' ? 'bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400' : 
-                          'bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400'
+                          file.status === 'ready' ? 'bg-green-100 text-green-700 dark:bg-green-900/40 dark:text-green-300' :
+                          file.status === 'processing' ? 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900/40 dark:text-yellow-300' :
+                          file.status === 'done' ? 'bg-blue-100 text-blue-700 dark:bg-blue-900/40 dark:text-blue-300' : 
+                          'bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-300'
                         }`}>
                           {file.status === 'ready' ? '准备就绪' :
                            file.status === 'processing' ? '处理中' :
@@ -302,7 +302,7 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
                     <button
                       onClick={() => handlePreview(file)}
                       disabled={isProcessing || isGeneratingPreview}
-                      className="text-green-500 dark:text-green-400 hover:text-green-700 dark:hover:text-green-300 hover:bg-green-50 dark:hover:bg-green-900 p-2 rounded-xl transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
+                      className="text-green-500 dark:text-green-400 hover:text-green-700 dark:hover:text-green-300 hover:bg-green-50 dark:hover:bg-green-900/30 p-2 rounded-xl transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
                       title="预览压缩效果"
                     >
                       <Eye className="h-4 w-4" />
@@ -310,7 +310,7 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
                     <button
                       onClick={() => removeFile(index)}
                       disabled={isProcessing}
-                      className="text-red-500 dark:text-red-400 hover:text-red-700 dark:hover:text-red-300 hover:bg-red-50 dark:hover:bg-red-900 p-2 rounded-xl transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
+                      className="text-red-500 dark:text-red-400 hover:text-red-700 dark:hover:text-red-300 hover:bg-red-50 dark:hover:bg-red-900/30 p-2 rounded-xl transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
                     >
                       删除
                     </button>

--- FILE: src/components/pdf-merger.tsx ---
diff --git a/src/components/pdf-merger.tsx b/src/components/pdf-merger.tsx
index 14bf7a8..4268518 100644
--- a/src/components/pdf-merger.tsx
+++ b/src/components/pdf-merger.tsx
@@ -308,10 +308,10 @@ export function PdfMerger({ isOpen, onClose }: PdfMergerProps) {
           </div>
         </div>
 
-        {/* 文件列表 */}
+        {/* 文件列表 - 优化深色模式样式 */}
         {files.length > 0 && (
-          <div className="border border-gray-200 dark:border-gray-700 rounded-2xl overflow-hidden">
-            <div className="p-4 bg-gray-50 dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700">
+          <div className="border border-gray-200 dark:border-gray-600 rounded-2xl overflow-hidden bg-white dark:bg-gray-700/50">
+            <div className="p-4 bg-gray-50 dark:bg-gray-700/70 border-b border-gray-200 dark:border-gray-600">
               <div className="flex justify-between items-center">
                 <div className="font-medium text-gray-900 dark:text-gray-100">
                   已选择的文件 ({files.length}/10) - 总大小: {(files.reduce((sum, f) => sum + f.size, 0) / 1024 / 1024).toFixed(2)} MB
@@ -324,7 +324,7 @@ export function PdfMerger({ isOpen, onClose }: PdfMergerProps) {
                 )}
               </div>
             </div>
-            <ul className="divide-y divide-gray-200 dark:divide-gray-700">
+            <ul className="divide-y divide-gray-200 dark:divide-gray-600">
               {files.map((file, index) => (
                 <li 
                   key={file.id} 
@@ -334,12 +334,12 @@ export function PdfMerger({ isOpen, onClose }: PdfMergerProps) {
                   onDragLeave={handleDragLeave}
                   onDrop={(e) => handleDrop(e, index)}
                   onDragEnd={handleDragEnd}
-                  className={`flex items-center justify-between p-4 transition-all duration-200 ${
+                  className={`flex items-center justify-between p-4 transition-all duration-200 bg-white dark:bg-transparent ${
                     draggedIndex === index 
                       ? 'opacity-50 bg-blue-50 dark:bg-blue-900/20 border-2 border-dashed border-blue-300 dark:border-blue-600' 
                       : dragOverIndex === index 
                         ? 'bg-blue-50 dark:bg-blue-900/20 border-t-2 border-blue-400 dark:border-blue-500' 
-                        : 'hover:bg-gray-50 dark:hover:bg-gray-800'
+                        : 'hover:bg-gray-50 dark:hover:bg-gray-600/30'
                   } ${!isProcessing ? 'cursor-move' : ''}`}
                 >
                   <div className="flex items-center space-x-3">
@@ -352,10 +352,10 @@ export function PdfMerger({ isOpen, onClose }: PdfMergerProps) {
                       <GripVertical className={`h-4 w-4 ${!isProcessing ? 'text-gray-400 dark:text-gray-500' : 'text-gray-300 dark:text-gray-600'}`} />
                     </div>
                     <div className={`p-2 rounded-xl ${
-                      file.status === 'ready' ? 'bg-blue-100 dark:bg-blue-900' :
-                      file.status === 'invalid' ? 'bg-red-100 dark:bg-red-900' :
-                      file.status === 'processing' ? 'bg-yellow-100 dark:bg-yellow-900' :
-                      file.status === 'done' ? 'bg-green-100 dark:bg-green-900' : 'bg-red-100 dark:bg-red-900'
+                      file.status === 'ready' ? 'bg-blue-100 dark:bg-blue-900/50' :
+                      file.status === 'invalid' ? 'bg-red-100 dark:bg-red-900/50' :
+                      file.status === 'processing' ? 'bg-yellow-100 dark:bg-yellow-900/50' :
+                      file.status === 'done' ? 'bg-green-100 dark:bg-green-900/50' : 'bg-red-100 dark:bg-red-900/50'
                     }`}>
                       <FileText className={`h-5 w-5 ${
                         file.status === 'ready' ? 'text-blue-600 dark:text-blue-400' :
@@ -378,7 +378,7 @@ export function PdfMerger({ isOpen, onClose }: PdfMergerProps) {
                   </div>
                   <button
                     onClick={() => removeFile(file.id)}
-                    className="text-red-500 dark:text-red-400 hover:text-red-700 dark:hover:text-red-300 hover:bg-red-50 dark:hover:bg-red-900 p-2 rounded-xl transition-all duration-200"
+                    className="text-red-500 dark:text-red-400 hover:text-red-700 dark:hover:text-red-300 hover:bg-red-50 dark:hover:bg-red-900/30 p-2 rounded-xl transition-all duration-200"
                   >
                     删除
                   </button>

--- FILE: src/ui/image-preview.tsx ---
diff --git a/src/ui/image-preview.tsx b/src/ui/image-preview.tsx
index acb888c..17f332c 100644
--- a/src/ui/image-preview.tsx
+++ b/src/ui/image-preview.tsx
@@ -1,6 +1,7 @@
 import React from 'react';
-import { X, Download, FileImage, Zap } from 'lucide-react';
+import { Download, FileImage, Zap } from 'lucide-react';
 import { formatFileSize } from '../lib/utils';
+import { Modal } from './modal';
 
 interface ImagePreviewProps {
   isOpen: boolean;
@@ -36,171 +37,156 @@ export function ImagePreview({
   onConfirm, 
   isGenerating 
 }: ImagePreviewProps) {
-  if (!isOpen) return null;
+  // 底部操作栏
+  const footerContent = (
+    <div className="flex justify-end space-x-3">
+      <button
+        onClick={onClose}
+        className="px-6 py-3 border border-gray-300 dark:border-gray-600 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700 font-medium transition-colors duration-200 text-gray-900 dark:text-gray-100"
+      >
+        取消
+      </button>
+      <button
+        onClick={onConfirm}
+        disabled={!preview || isGenerating}
+        className="px-6 py-3 bg-blue-600 dark:bg-blue-500 text-white rounded-xl font-medium hover:bg-blue-700 dark:hover:bg-blue-600 disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-200 flex items-center space-x-2"
+      >
+        <Download className="h-4 w-4" />
+        <span>确认压缩</span>
+      </button>
+    </div>
+  );
 
   return (
-    <div className="fixed inset-0 bg-black bg-opacity-50 dark:bg-black dark:bg-opacity-70 flex items-center justify-center z-[60] p-4">
-      <div className="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl max-w-6xl w-full max-h-[90vh] overflow-hidden">
-        {/* 头部 */}
-        <div className="flex items-center justify-between p-6 border-b border-gray-200 dark:border-gray-700">
-          <div className="flex items-center space-x-3">
-            <div className="p-2 bg-blue-100 dark:bg-blue-900 rounded-xl">
-              <FileImage className="h-6 w-6 text-blue-600 dark:text-blue-400" />
-            </div>
-            <div>
-              <h2 className="text-xl font-semibold text-gray-900 dark:text-gray-100">压缩预览</h2>
-              <p className="text-sm text-gray-500 dark:text-gray-400">查看压缩前后的效果对比</p>
-            </div>
+    <Modal 
+      isOpen={isOpen} 
+      onClose={onClose} 
+      title="压缩预览" 
+      footer={footerContent}
+      size="extra-large"
+      zIndex={60}
+    >
+      <div className="space-y-6">
+        {isGenerating ? (
+          <div className="flex flex-col items-center justify-center py-12">
+            <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 dark:border-blue-400 mb-4"></div>
+            <p className="text-gray-600 dark:text-gray-400">正在生成预览...</p>
           </div>
-          <button
-            onClick={onClose}
-            className="p-2 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-xl transition-colors duration-200"
-          >
-            <X className="h-6 w-6 text-gray-500 dark:text-gray-400" />
-          </button>
-        </div>
-
-        {/* 内容区域 */}
-        <div className="p-6 overflow-y-auto max-h-[calc(90vh-200px)]">
-          {isGenerating ? (
-            <div className="flex flex-col items-center justify-center py-12">
-              <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 dark:border-blue-400 mb-4"></div>
-              <p className="text-gray-600 dark:text-gray-400">正在生成预览...</p>
-            </div>
-          ) : preview ? (
-            <div className="space-y-6">
-              {/* 统计信息卡片 */}
-              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
-                <div className="bg-gray-50 dark:bg-gray-700 rounded-xl p-4">
-                  <div className="flex items-center space-x-2 mb-2">
-                    <div className="w-3 h-3 bg-red-500 rounded-full"></div>
-                    <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩前</span>
-                  </div>
-                  <p className="text-2xl font-bold text-gray-900 dark:text-gray-100">
-                    {formatFileSize(preview.original.size)}
-                  </p>
-                  <p className="text-sm text-gray-500 dark:text-gray-400">
-                    {preview.original.width} × {preview.original.height}
-                  </p>
+        ) : preview ? (
+          <div className="space-y-6">
+            {/* 统计信息卡片 */}
+            <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
+              <div className="bg-gray-50 dark:bg-gray-700 rounded-xl p-4">
+                <div className="flex items-center space-x-2 mb-2">
+                  <div className="w-3 h-3 bg-red-500 rounded-full"></div>
+                  <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩前</span>
                 </div>
-                
-                <div className="bg-green-50 dark:bg-green-900 rounded-xl p-4">
-                  <div className="flex items-center space-x-2 mb-2">
-                    <div className="w-3 h-3 bg-green-500 rounded-full"></div>
-                    <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩后</span>
-                  </div>
-                  <p className="text-2xl font-bold text-gray-900 dark:text-gray-100">
-                    {formatFileSize(preview.compressed.size)}
-                  </p>
-                  <p className="text-sm text-gray-500 dark:text-gray-400">
-                    {preview.compressed.width} × {preview.compressed.height}
-                  </p>
+                <p className="text-2xl font-bold text-gray-900 dark:text-gray-100">
+                  {formatFileSize(preview.original.size)}
+                </p>
+                <p className="text-sm text-gray-500 dark:text-gray-400">
+                  {preview.original.width} × {preview.original.height}
+                </p>
+              </div>
+              
+              <div className="bg-green-50 dark:bg-green-900 rounded-xl p-4">
+                <div className="flex items-center space-x-2 mb-2">
+                  <div className="w-3 h-3 bg-green-500 rounded-full"></div>
+                  <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩后</span>
                 </div>
-                
-                <div className="bg-blue-50 dark:bg-blue-900 rounded-xl p-4">
-                  <div className="flex items-center space-x-2 mb-2">
-                    <Zap className="w-4 h-4 text-blue-600 dark:text-blue-400" />
-                    <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩效果</span>
-                  </div>
-                  <p className="text-2xl font-bold text-blue-600 dark:text-blue-400">
-                    {preview.compressionRatio > 0 ? '-' : '+'}{Math.abs(preview.compressionRatio)}%
-                  </p>
-                  <p className="text-sm text-gray-500 dark:text-gray-400">
-                    {preview.sizeDifference > 0 ? '节省' : '增加'} {formatFileSize(Math.abs(preview.sizeDifference))}
-                  </p>
+                <p className="text-2xl font-bold text-gray-900 dark:text-gray-100">
+                  {formatFileSize(preview.compressed.size)}
+                </p>
+                <p className="text-sm text-gray-500 dark:text-gray-400">
+                  {preview.compressed.width} × {preview.compressed.height}
+                </p>
+              </div>
+              
+              <div className="bg-blue-50 dark:bg-blue-900 rounded-xl p-4">
+                <div className="flex items-center space-x-2 mb-2">
+                  <Zap className="w-4 h-4 text-blue-600 dark:text-blue-400" />
+                  <span className="text-sm font-medium text-gray-700 dark:text-gray-300">压缩效果</span>
                 </div>
+                <p className="text-2xl font-bold text-blue-600 dark:text-blue-400">
+                  {preview.compressionRatio > 0 ? '-' : '+'}{Math.abs(preview.compressionRatio)}%
+                </p>
+                <p className="text-sm text-gray-500 dark:text-gray-400">
+                  {preview.sizeDifference > 0 ? '节省' : '增加'} {formatFileSize(Math.abs(preview.sizeDifference))}
+                </p>
               </div>
+            </div>
 
-              {/* 图片对比 */}
-              <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
-                {/* 原图 */}
-                <div className="space-y-3">
-                  <div className="flex items-center justify-between">
-                    <h3 className="text-lg font-medium text-gray-900 dark:text-gray-100">原图</h3>
-                    <span className="text-sm text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">
-                      {preview.original.format.toUpperCase()}
-                    </span>
-                  </div>
-                  <div className="border-2 border-gray-200 dark:border-gray-600 rounded-xl overflow-hidden bg-gray-50 dark:bg-gray-700">
-                    <img
-                      src={preview.originalBase64}
-                      alt="原图"
-                      className="w-full h-auto max-h-80 object-contain"
-                      style={{ imageRendering: 'auto' }}
-                    />
-                  </div>
+            {/* 图片对比 */}
+            <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
+              {/* 原图 */}
+              <div className="space-y-3">
+                <div className="flex items-center justify-between">
+                  <h3 className="text-lg font-medium text-gray-900 dark:text-gray-100">原图</h3>
+                  <span className="text-sm text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">
+                    {preview.original.format.toUpperCase()}
+                  </span>
                 </div>
-
-                {/* 压缩后 */}
-                <div className="space-y-3">
-                  <div className="flex items-center justify-between">
-                    <h3 className="text-lg font-medium text-gray-900 dark:text-gray-100">压缩后</h3>
-                    <span className="text-sm text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">
-                      {preview.compressed.format.toUpperCase()}
-                    </span>
-                  </div>
-                  <div className="border-2 border-green-200 dark:border-green-600 rounded-xl overflow-hidden bg-gray-50 dark:bg-gray-700">
-                    <img
-                      src={preview.compressedBase64}
-                      alt="压缩后"
-                      className="w-full h-auto max-h-80 object-contain"
-                      style={{ imageRendering: 'auto' }}
-                    />
-                  </div>
+                <div className="border-2 border-gray-200 dark:border-gray-600 rounded-xl overflow-hidden bg-gray-50 dark:bg-gray-700">
+                  <img
+                    src={preview.originalBase64}
+                    alt="原图"
+                    className="w-full h-auto max-h-80 object-contain"
+                    style={{ imageRendering: 'auto' }}
+                  />
                 </div>
               </div>
 
-              {/* 详细信息 */}
-              <div className="bg-gray-50 dark:bg-gray-700 rounded-xl p-4">
-                <h4 className="font-medium text-gray-900 dark:text-gray-100 mb-3">详细信息</h4>
-                <div className="grid grid-cols-2 gap-4 text-sm">
-                  <div>
-                    <span className="text-gray-600 dark:text-gray-400">原始格式:</span>
-                    <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.original.format.toUpperCase()}</span>
-                  </div>
-                  <div>
-                    <span className="text-gray-600 dark:text-gray-400">输出格式:</span>
-                    <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.compressed.format.toUpperCase()}</span>
-                  </div>
-                  <div>
-                    <span className="text-gray-600 dark:text-gray-400">原始尺寸:</span>
-                    <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.original.width} × {preview.original.height}</span>
-                  </div>
-                  <div>
-                    <span className="text-gray-600 dark:text-gray-400">压缩后尺寸:</span>
-                    <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.compressed.width} × {preview.compressed.height}</span>
-                  </div>
+              {/* 压缩后 */}
+              <div className="space-y-3">
+                <div className="flex items-center justify-between">
+                  <h3 className="text-lg font-medium text-gray-900 dark:text-gray-100">压缩后</h3>
+                  <span className="text-sm text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">
+                    {preview.compressed.format.toUpperCase()}
+                  </span>
+                </div>
+                <div className="border-2 border-green-200 dark:border-green-600 rounded-xl overflow-hidden bg-gray-50 dark:bg-gray-700">
+                  <img
+                    src={preview.compressedBase64}
+                    alt="压缩后"
+                    className="w-full h-auto max-h-80 object-contain"
+                    style={{ imageRendering: 'auto' }}
+                  />
                 </div>
               </div>
             </div>
-          ) : (
-            <div className="flex flex-col items-center justify-center py-12">
-              <div className="p-4 bg-gray-100 dark:bg-gray-700 rounded-2xl mb-4">
-                <FileImage className="h-12 w-12 text-gray-400" />
+
+            {/* 详细信息 */}
+            <div className="bg-gray-50 dark:bg-gray-700 rounded-xl p-4">
+              <h4 className="font-medium text-gray-900 dark:text-gray-100 mb-3">详细信息</h4>
+              <div className="grid grid-cols-2 gap-4 text-sm">
+                <div>
+                  <span className="text-gray-600 dark:text-gray-400">原始格式:</span>
+                  <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.original.format.toUpperCase()}</span>
+                </div>
+                <div>
+                  <span className="text-gray-600 dark:text-gray-400">输出格式:</span>
+                  <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.compressed.format.toUpperCase()}</span>
+                </div>
+                <div>
+                  <span className="text-gray-600 dark:text-gray-400">原始尺寸:</span>
+                  <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.original.width} × {preview.original.height}</span>
+                </div>
+                <div>
+                  <span className="text-gray-600 dark:text-gray-400">压缩后尺寸:</span>
+                  <span className="ml-2 font-medium text-gray-900 dark:text-gray-100">{preview.compressed.width} × {preview.compressed.height}</span>
+                </div>
               </div>
-              <p className="text-gray-600 dark:text-gray-400">无法生成预览</p>
             </div>
-          )}
-        </div>
-
-        {/* 底部操作栏 */}
-        <div className="flex justify-end space-x-3 p-6 border-t border-gray-200 dark:border-gray-700">
-          <button
-            onClick={onClose}
-            className="px-6 py-3 border border-gray-300 dark:border-gray-600 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700 font-medium transition-colors duration-200 text-gray-900 dark:text-gray-100"
-          >
-            取消
-          </button>
-          <button
-            onClick={onConfirm}
-            disabled={!preview || isGenerating}
-            className="px-6 py-3 bg-blue-600 dark:bg-blue-500 text-white rounded-xl font-medium hover:bg-blue-700 dark:hover:bg-blue-600 disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-200 flex items-center space-x-2"
-          >
-            <Download className="h-4 w-4" />
-            <span>确认压缩</span>
-          </button>
-        </div>
+          </div>
+        ) : (
+          <div className="flex flex-col items-center justify-center py-12">
+            <div className="p-4 bg-gray-100 dark:bg-gray-700 rounded-2xl mb-4">
+              <FileImage className="h-12 w-12 text-gray-400" />
+            </div>
+            <p className="text-gray-600 dark:text-gray-400">无法生成预览</p>
+          </div>
+        )}
       </div>
-    </div>
+    </Modal>
   );
 } 
\ No newline at end of file

--- FILE: src/ui/modal.tsx ---
diff --git a/src/ui/modal.tsx b/src/ui/modal.tsx
index 8d7cf19..b31d24f 100644
--- a/src/ui/modal.tsx
+++ b/src/ui/modal.tsx
@@ -1,4 +1,4 @@
-import React from 'react';
+import React, { useEffect } from 'react';
 import { X } from 'lucide-react';
 
 interface ModalProps {
@@ -7,14 +7,51 @@ interface ModalProps {
   title: string;
   children?: React.ReactNode;
   footer?: React.ReactNode;
+  size?: 'default' | 'large' | 'extra-large';
+  zIndex?: number;
 }
 
-export function Modal({ isOpen, onClose, title, children, footer }: ModalProps) {
+export function Modal({ isOpen, onClose, title, children, footer, size = 'default', zIndex = 50 }: ModalProps) {
+  // ESC键关闭模态框
+  useEffect(() => {
+    const handleEscapeKey = (event: KeyboardEvent) => {
+      if (event.key === 'Escape' && isOpen) {
+        onClose();
+      }
+    };
+
+    if (isOpen) {
+      document.addEventListener('keydown', handleEscapeKey);
+      // 防止背景滚动
+      document.body.style.overflow = 'hidden';
+    }
+
+    return () => {
+      document.removeEventListener('keydown', handleEscapeKey);
+      document.body.style.overflow = 'unset';
+    };
+  }, [isOpen, onClose]);
+
   if (!isOpen) return null;
 
+  // 点击遮罩关闭模态框
+  const handleOverlayClick = (e: React.MouseEvent) => {
+    if (e.target === e.currentTarget) {
+      onClose();
+    }
+  };
+
   return (
-    <div className="fixed inset-0 bg-black bg-opacity-50 dark:bg-black dark:bg-opacity-80 flex items-center justify-center z-50 p-4">
-      <div className="bg-white dark:bg-gray-700 rounded-2xl shadow-2xl border border-gray-200 dark:border-gray-600 ring-1 ring-gray-900/5 dark:ring-gray-100/10 w-full max-w-3xl max-h-[90vh] flex flex-col">
+    <div 
+      className="fixed inset-0 bg-black bg-opacity-50 dark:bg-black dark:bg-opacity-80 flex items-center justify-center p-4"
+      style={{ zIndex }}
+      onClick={handleOverlayClick}
+    >
+      <div className={`bg-white dark:bg-gray-700 rounded-2xl shadow-2xl border border-gray-200 dark:border-gray-600 ring-1 ring-gray-900/5 dark:ring-gray-100/10 w-full max-h-[90vh] flex flex-col ${
+        size === 'extra-large' ? 'max-w-6xl' : 
+        size === 'large' ? 'max-w-5xl' : 
+        'max-w-3xl'
+      }`}>
         {/* 固定顶部标题栏 */}
         <div className="flex justify-between items-center p-6 border-b border-gray-200 dark:border-gray-700 flex-shrink-0">
           <h2 className="text-xl font-semibold text-gray-900 dark:text-gray-100">{title}</h2>
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.