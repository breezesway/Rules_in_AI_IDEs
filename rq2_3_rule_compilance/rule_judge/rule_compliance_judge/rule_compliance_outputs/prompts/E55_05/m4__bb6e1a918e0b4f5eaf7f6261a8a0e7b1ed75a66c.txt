- Rule Primary Category: Architecture & Design
- Rule Secondary Category: Design References & Constraints
- Rule File Path: .trae/rules/project_rules.md
- Change Type: added
- Target Rule Content:
6. 不要混淆前后端的`gameState`，UI使用`displayGameState`，后端使用`backendGameState`。

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: Source code files (variable names, object properties, and type definitions) across the repository.
- Suggested Logic: 1. Search for any usage of the generic identifier 'gameState' and flag it as a violation of the rule to separate the two types. 2. Verify that identifiers named 'displayGameState' are used in files associated with the UI (e.g., directories like /ui, /components, /frontend). 3. Verify that identifiers named 'backendGameState' are used in files associated with the backend (e.g., /api, /server, /backend).
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=4)
- Commit Message: refactor: simplify enemy action methods and improve animation handling
- Changed Files List: 
src/data/enemies/boss.js	+28	-58	after_lines=82
src/data/enemies/slimes.js	+1	-4	after_lines=43
src/data/enemies/special.js	+17	-29	after_lines=55

- Project Directory Tree (Snapshot after commit):
├── .github
│   └── workflows
│       └── main.yml
├── .gitignore
├── .idea
│   ├── .gitignore
│   ├── codeStyles
│   │   └── codeStyleConfig.xml
│   ├── copilot.data.migration.agent.xml
│   ├── copilot.data.migration.ask.xml
│   ├── copilot.data.migration.ask2agent.xml
│   ├── copilot.data.migration.edit.xml
│   ├── modules.xml
│   ├── rtvl_test.iml
│   ├── vcs.xml
│   └── wekyspire.iml
├── .trae
│   └── rules
│       └── project_rules.md
├── README.md
├── index.html
├── package-lock.json
├── package.json
├── public
│   └── changelog.md
├── src
│   ├── App.vue
│   ├── DebugApp.vue
│   ├── GameApp.vue
│   ├── assets
│   │   ├── common.css
│   │   ├── cutscenes
│   │   │   ├── opening-1.png
│   │   │   └── opening-2.png
│   │   ├── enemies
│   │   │   └── slime.png
│   │   ├── images
│   │   │   ├── shop-background.png
│   │   │   └── start-screen.png
│   │   ├── main.css
│   │   ├── remi.png
│   │   └── sounds
│   │       └── story-mode-intro.mp3
│   ├── backendEventBus.js
│   ├── components
│   │   ├── AbilityRewardPanel.vue
│   │   ├── ActionPointsBar.vue
│   │   ├── ArenaDebugScreen.vue
│   │   ├── AudioControllerScreen.vue
│   │   ├── BarPoint.vue
│   │   ├── BattleLogPanel.vue
│   │   ├── BattleScreen.vue
│   │   ├── BossShowupAnimation.vue
│   │   ├── BreakthroughRewardPanel.vue
│   │   ├── ChangeLog.vue
│   │   ├── ColoredText.vue
│   │   ├── CutsceneScreen.vue
│   │   ├── DialogScreen.vue
│   │   ├── EffectDisplayBar.vue
│   │   ├── EffectIcon.vue
│   │   ├── EndScreen.vue
│   │   ├── EnemyStatusPanel.vue
│   │   ├── HealthBar.vue
│   │   ├── HurtAnimationWrapper.vue
│   │   ├── ManaBar.vue
│   │   ├── MessagePopupScreen.vue
│   │   ├── MoneyRewardPanel.vue
│   │   ├── NamedEntity.vue
│   │   ├── NamedEntityTest.vue
│   │   ├── ParticleEffectManager.vue
│   │   ├── PlayerBasicStats.vue
│   │   ├── PlayerStatusPanel.vue
│   │   ├── RestScreen.vue
│   │   ├── ShopPanel.vue
│   │   ├── SkillCard.vue
│   │   ├── SkillRewardPanel.vue
│   │   ├── SkillSlot.vue
│   │   ├── SkillSlotSelectionPanel.vue
│   │   ├── StartScreen.vue
│   │   └── TestEffectDisplay.vue
│   ├── data
│   │   ├── README.md
│   │   ├── ability.js
│   │   ├── abilityManager.js
│   │   ├── animationDispatcher.js
│   │   ├── battle.js
│   │   ├── battleLogUtils.js
│   │   ├── battleUtils.js
│   │   ├── dialogues.js
│   │   ├── effect.js
│   │   ├── effectDescription.js
│   │   ├── effectProcessor.js
│   │   ├── enemies
│   │   │   ├── README.md
│   │   │   ├── basic.js
│   │   │   ├── boss.js
│   │   │   ├── slimes.js
│   │   │   └── special.js
│   │   ├── enemy.js
│   │   ├── enemyFactory.js
│   │   ├── events.md
│   │   ├── gameState.js
│   │   ├── item.js
│   │   ├── itemManager.js
│   │   ├── namedEntities.js
│   │   ├── player.js
│   │   ├── rest.js
│   │   ├── skill.js
│   │   ├── skillManager.js
│   │   ├── skills
│   │   │   ├── README.md
│   │   │   ├── basic.js
│   │   │   ├── blast.js
│   │   │   ├── cMinus.js
│   │   │   ├── concentration.js
│   │   │   ├── curses.js
│   │   │   ├── fireAssist.js
│   │   │   ├── heal.js
│   │   │   ├── punchKicks.js
│   │   │   ├── shielding.js
│   │   │   └── untested
│   │   │       ├── firecontrol.js
│   │   │       ├── icecontrol.js
│   │   │       ├── lumi.js
│   │   │       └── remi.js
│   │   ├── testSkill.js
│   │   └── unit.js
│   ├── eventBus.js
│   ├── frontendEventBus.js
│   ├── main.js
│   └── utils
│       └── tierUtils.js
└── vite.config.js

- File Diffs (Code Changes in this Commit):
--- FILE: src/data/enemies/boss.js ---
diff --git a/src/data/enemies/boss.js b/src/data/enemies/boss.js
index 222da93..001a615 100644
--- a/src/data/enemies/boss.js
+++ b/src/data/enemies/boss.js
@@ -1,6 +1,7 @@
 import Enemy from '../enemy.js';
 import { launchAttack } from '../battleUtils.js';
 import { addEnemyActionLog } from '../battleLogUtils.js';
+import {enqueueDelay} from "../animationDispatcher";
 
 // MEFM-3 Boss敌人
 export class MEFM3 extends Enemy {
@@ -21,13 +22,13 @@ export class MEFM3 extends Enemy {
   }
 
   // 执行行动
-  act(player, battleLogs) {
+  act(player) {
     // 准备阶段
     if (!this.prepared) {
       this.prepared = true;
       this.addEffect('高燃弹药', 1);
       addEnemyActionLog(`${this.name} 完成了弹药装载，要来了！`);
-      return Promise.resolve();
+      return;
     }
     
     // 检查是否达到生命值阈值
@@ -35,78 +36,47 @@ export class MEFM3 extends Enemy {
       this.hpThresholdReached = true;
       addEnemyActionLog(`${this.name} 承受了太多伤害，陷入了故障状态！`);
       this.addEffect('眩晕', 1);
-      return Promise.resolve();
+      return;
     }
     
     // 正常行动序列
     const actions = [
       () => {
-        return new Promise((resolve) => {
-          addEnemyActionLog(`${this.name} 使用射流机枪扫射！`);
-          const times = 2 + (this.effects['机枪升温'] || 0);
-          const damage = 1 + this.attack;
-          // 逐个执行攻击，并在每次攻击之间添加延时
-          let i = 0;
-          const executeAttack = () => {
-            if (i < times) {
-              launchAttack(this, player, damage);
-              i++;
-              // 添加延时后再执行下一次攻击
-              setTimeout(executeAttack, 800);
-            } else {
-              // 所有攻击执行完毕
-              resolve();
-            }
-          };
-          executeAttack();
-        });
+        addEnemyActionLog(`${this.name} 使用射流机枪扫射！`);
+        const times = 2 + (this.effects['机枪升温'] || 0);
+        const damage = 1 + this.attack;
+        // 逐个执行攻击，并在每次攻击之间添加延时
+        for(let i = 0; i < times; i++) {
+          launchAttack(this, player, damage);
+          enqueueDelay(800);
+        }
       },
       () => {
         if(Math.random() < 0.5) {
-          return new Promise((resolve) => {
-            addEnemyActionLog(`${this.name} 使用射流机枪连续射击！`);
-            const times = 2 + (this.effects['机枪升温'] || 0);
-            const damage = 1 + this.attack;
-            // 逐个执行攻击，并在每次攻击之间添加延时
-            let i = 0;
-            const executeAttack = () => {
-              if (i < times) {
-                launchAttack(this, player, damage);
-                i++;
-                // 添加延时后再执行下一次攻击
-                setTimeout(executeAttack, 800);
-              } else {
-                // 所有攻击执行完毕
-                resolve();
-              }
-            };
-            executeAttack();
-          });
+          addEnemyActionLog(`${this.name} 使用射流机枪连续射击！`);
+          const times = 2 + (this.effects['机枪升温'] || 0);
+          const damage = 1 + this.attack;
+          // 逐个执行攻击，并在每次攻击之间添加延时
+          for(let i = 0; i < times; i++) {
+            enqueueDelay(800);
+            launchAttack(this, player, damage);
+          }
         } else {
-          return new Promise((resolve) => {
-            addEnemyActionLog(`${this.name} 的钩爪抓住了你，你的防御变得薄弱。`);
-            player.addEffect('坚固', -2);
-            setTimeout(() =>{resolve();}, 1000);
-          });
+          addEnemyActionLog(`${this.name} 的钩爪抓住了你，你的防御变得薄弱。`);
+          player.addEffect('坚固', -2);
         }
       },
       () => {
-        return new Promise((resolve) => {
-          addEnemyActionLog(`${this.name} 启动能量耦合，泄露的魏启在空气中扭曲，它周遭的温度又升高了。`);
-          this.addEffect('机枪升温', 1);
-          const X = () => {
-            addEnemyActionLog(`${this.name} 完成了装甲强化。`);
-            this.addEffect('格挡', 2);
-            resolve();
-          };
-          setTimeout(X, 1000);
-        });
+        addEnemyActionLog(`${this.name} 启动能量耦合，泄露的魏启在空气中扭曲，它周遭的温度又升高了。`);
+        this.addEffect('机枪升温', 1);
+        enqueueDelay(1000);
+        addEnemyActionLog(`${this.name} 完成了装甲强化。`);
+        this.addEffect('格挡', 2);
       }
     ];
     
     const action = actions[this.actionIndex % actions.length];
+    action();
     this.actionIndex++;
-    
-    return action();
   }
 }

--- FILE: src/data/enemies/slimes.js ---
diff --git a/src/data/enemies/slimes.js b/src/data/enemies/slimes.js
index f8713b5..2fa1905 100644
--- a/src/data/enemies/slimes.js
+++ b/src/data/enemies/slimes.js
@@ -23,7 +23,7 @@ class FireSlime extends Enemy {
   }
 
   // 敌人行动方法
-  act(player, battleLogs) {
+  act(player) {
     // 火史莱姆有50%概率使用普通攻击，50%概率使用燃烧攻击
     if (Math.random() < 0.5) {
       // 普通攻击
@@ -37,9 +37,6 @@ class FireSlime extends Enemy {
       const result = launchAttack(this, player, damage);
       if(result.passThoughDamage > 0) player.addEffect('燃烧', 4);
     }
-    
-    // 返回Promise以适配新的act方法
-    return Promise.resolve();
   }
 }

--- FILE: src/data/enemies/special.js ---
diff --git a/src/data/enemies/special.js b/src/data/enemies/special.js
index 94b2b7a..e3fc7b0 100644
--- a/src/data/enemies/special.js
+++ b/src/data/enemies/special.js
@@ -1,6 +1,7 @@
 import Enemy from '../enemy.js';
 import { launchAttack } from '../battleUtils.js';
 import { addEnemyActionLog } from '../battleLogUtils.js';
+import {enqueueDelay} from "../animationDispatcher";
 
 // 雪狼 精英敌人
 export class BigWolf extends Enemy {
@@ -16,52 +17,39 @@ export class BigWolf extends Enemy {
     }
 
     // 执行行动
-    act(player, battleLogs) {
+    act(player) {
 
         this.actRounds += 1;
         // 在第二回合为玩家增加虚弱效果
         if (this.actRounds === 2) {
             addEnemyActionLog(`${this.name} 发出一生怒吼，你双股战战几欲跌倒！`);
             this.addEffect('虚弱', 2);
-            return Promise.resolve();
+            return ;
         }
 
         // 正常行动序列
         const actions = [
             () => {
-                return new Promise((resolve) => {
-                    addEnemyActionLog(`${this.name} 冲撞攻击！`);
-                    const damage = 1 + this.attack;
-                    launchAttack(this, player, damage);
-                    resolve();
-                });
+              addEnemyActionLog(`${this.name} 冲撞攻击！`);
+              const damage = 1 + this.attack;
+              launchAttack(this, player, damage);
             },
             () => {
-                return new Promise((resolve) => {
-                    addEnemyActionLog(`${this.name} 连续撕咬。`);
-                    const times = 2 + ((Math.random() > 0.5) ? 1 : 0);
-                    const damage = 1 + this.attack / 2;
-                    // 逐个执行攻击，并在每次攻击之间添加延时
-                    let i = 0;
-                    const executeAttack = () => {
-                        if (i < times) {
-                            launchAttack(this, player, damage);
-                            i++;
-                            // 添加延时后再执行下一次攻击
-                            setTimeout(executeAttack, 1200);
-                        } else {
-                            // 所有攻击执行完毕
-                            resolve();
-                        }
-                    };
-                    executeAttack();
-                });
+              addEnemyActionLog(`${this.name} 连续撕咬。`);
+              const times = 2 + ((Math.random() > 0.5) ? 1 : 0);
+              const damage = 1 + this.attack / 2;
+              // 逐个执行攻击，并在每次攻击之间添加延时
+              for(var i = 0; i < times; i++) {
+                  launchAttack(this, player, damage);
+                  // 添加延时后再执行下一次攻击
+                  enqueueDelay(1200);
+              }
             }
         ];
 
         const action = actions[this.actionIndex % actions.length];
+        action();
         this.actionIndex++;
-
-        return action();
+        return;
     }
 }
\ No newline at end of file
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.