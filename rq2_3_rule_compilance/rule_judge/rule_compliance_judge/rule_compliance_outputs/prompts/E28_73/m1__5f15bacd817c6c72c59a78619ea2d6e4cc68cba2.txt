- Rule Primary Category: Architecture & Design
- Rule Secondary Category: Technology Stack Selections
- Rule File Path: .kiro/steering/tech.md
- Change Type: added
- Target Rule Content:
- Package Management: npm with package-lock.json for dependency locking

[Detection Suggestions (FOR REFERENCE ONLY)]
- Suggested Targets: File system structure, specifically the root directory for configuration and lock files.
- Suggested Logic: Verify the existence of 'package-lock.json' in the project root. Simultaneously, check for the absence of conflicting lock files such as 'yarn.lock' or 'pnpm-lock.yaml' to ensure npm is the exclusive package manager.
*(Note: These targets and logic are programmatic guesses. Because rules are diverse, evaluate the specific context and decide the best verification method yourself).*

[Commit Information to Evaluate]
- Relative Position: after_rule_commit (distance=1)
- Commit Message: refactor: rename messageBus service file and update import paths
- Changed Files List: 
web/src/services/message-bus.service.ts	+45	-0	after_lines=45
web/src/services/messageBus.ts	+0	-45	after_lines=0
web/src/views/Sidebar/Sidebar.tsx	+1	-1	after_lines=52

- Project Directory Tree (Snapshot after commit):
├── .gitignore
├── .kiro
│   └── steering
│       ├── product.md
│       ├── structure.md
│       └── tech.md
├── .vscode
│   ├── extensions.json
│   ├── launch.json
│   ├── settings.json
│   └── tasks.json
├── .vscode-test.mjs
├── .vscodeignore
├── CHANGELOG.md
├── LICENSE
├── README.md
├── docs
│   ├── bundle-architecture.md
│   ├── events.md
│   ├── extension-bus.md
│   ├── index.md
│   ├── lifecycle.md
│   ├── recipes.md
│   └── webview-bus.md
├── eslint.config.mjs
├── media
│   ├── global.css
│   └── kiro.svg
├── package-lock.json
├── package.json
├── src
│   ├── extension.ts
│   ├── mcp
│   │   ├── mcp-stdio.server.ts
│   │   └── mcp.provider.ts
│   ├── services
│   │   ├── http-bridge.service.ts
│   │   └── message-bus.service.ts
│   ├── shared
│   │   ├── commands.ts
│   │   ├── events.ts
│   │   └── utils
│   │       ├── event-bus-register.utils.ts
│   │       └── generate-nonce.utils.ts
│   ├── test
│   │   └── extension.test.ts
│   ├── types
│   │   └── mcp-shims.d.ts
│   └── ui-providers
│       ├── asset-manifest.ts
│       ├── health-dashboard
│       │   ├── health-dashboard.panel.ts
│       │   └── index.ts
│       └── sidebar
│           ├── index.ts
│           └── sidebar.provider.ts
├── tsconfig.json
└── web
    ├── src
    │   ├── components
    │   │   └── molecules
    │   │       └── Button.tsx
    │   ├── main-dashboard.tsx
    │   ├── main-sidebar.tsx
    │   ├── services
    │   │   └── message-bus.service.ts
    │   ├── types
    │   │   └── cssmodule.d.ts
    │   ├── views
    │   │   ├── HealthDashboard
    │   │   │   ├── HealthDashboard.module.css
    │   │   │   └── HealthDashboard.tsx
    │   │   └── Sidebar
    │   │       ├── Sidebar.module.css
    │   │       └── Sidebar.tsx
    │   └── vscode.ts
    ├── tsconfig.json
    └── vite.config.ts

- File Diffs (Code Changes in this Commit):
--- FILE: web/src/services/message-bus.service.ts ---
diff --git a/web/src/services/message-bus.service.ts b/web/src/services/message-bus.service.ts
new file mode 100644
index 0000000..76ad1ea
--- /dev/null
+++ b/web/src/services/message-bus.service.ts
@@ -0,0 +1,45 @@
+import { Events, EventType, BusEvent, EventPayloads } from '../../../src/shared/events';
+import { getVsCodeApi } from '../vscode';
+
+type Handler<K extends EventType = EventType> = (event: BusEvent<K>) => void | Promise<void>;
+
+class WebviewMessageBus {
+  private readonly vscode = getVsCodeApi<BusEvent>();
+  private readonly handlers = new Map<EventType, Set<Handler>>();
+
+  constructor() {
+    window.addEventListener('message', (e: MessageEvent<BusEvent>) => {
+      const msg = e.data;
+      if (!msg || typeof msg !== 'object' || !('type' in msg)) {
+        return;
+      }
+      const set = this.handlers.get(msg.type as EventType);
+      if (!set) {
+        return;
+      }
+      for (const h of set) {
+        Promise.resolve(h(msg as any));
+      }
+    });
+  }
+
+  on<K extends EventType>(type: K, handler: Handler<K>): () => void {
+    if (!this.handlers.has(type)) {
+      this.handlers.set(type, new Set());
+    }
+    const set = this.handlers.get(type)! as Set<Handler<K>>;
+    set.add(handler);
+    return () => set.delete(handler);
+  }
+
+  emit<K extends EventType>(type: K, payload: EventPayloads[K]): void {
+    this.vscode.postMessage({ type, payload } as BusEvent<K>);
+  }
+
+  // Alias for clarity
+  send = this.emit.bind(this);
+}
+
+export const events = Events;
+export const messageBus = new WebviewMessageBus();
+export type { EventType, BusEvent };

--- FILE: web/src/services/messageBus.ts ---
diff --git a/web/src/services/messageBus.ts b/web/src/services/messageBus.ts
deleted file mode 100644
index 76ad1ea..0000000
--- a/web/src/services/messageBus.ts
+++ /dev/null
@@ -1,45 +0,0 @@
-import { Events, EventType, BusEvent, EventPayloads } from '../../../src/shared/events';
-import { getVsCodeApi } from '../vscode';
-
-type Handler<K extends EventType = EventType> = (event: BusEvent<K>) => void | Promise<void>;
-
-class WebviewMessageBus {
-  private readonly vscode = getVsCodeApi<BusEvent>();
-  private readonly handlers = new Map<EventType, Set<Handler>>();
-
-  constructor() {
-    window.addEventListener('message', (e: MessageEvent<BusEvent>) => {
-      const msg = e.data;
-      if (!msg || typeof msg !== 'object' || !('type' in msg)) {
-        return;
-      }
-      const set = this.handlers.get(msg.type as EventType);
-      if (!set) {
-        return;
-      }
-      for (const h of set) {
-        Promise.resolve(h(msg as any));
-      }
-    });
-  }
-
-  on<K extends EventType>(type: K, handler: Handler<K>): () => void {
-    if (!this.handlers.has(type)) {
-      this.handlers.set(type, new Set());
-    }
-    const set = this.handlers.get(type)! as Set<Handler<K>>;
-    set.add(handler);
-    return () => set.delete(handler);
-  }
-
-  emit<K extends EventType>(type: K, payload: EventPayloads[K]): void {
-    this.vscode.postMessage({ type, payload } as BusEvent<K>);
-  }
-
-  // Alias for clarity
-  send = this.emit.bind(this);
-}
-
-export const events = Events;
-export const messageBus = new WebviewMessageBus();
-export type { EventType, BusEvent };

--- FILE: web/src/views/Sidebar/Sidebar.tsx ---
diff --git a/web/src/views/Sidebar/Sidebar.tsx b/web/src/views/Sidebar/Sidebar.tsx
index 2c9f7c6..84ac17c 100644
--- a/web/src/views/Sidebar/Sidebar.tsx
+++ b/web/src/views/Sidebar/Sidebar.tsx
@@ -1,7 +1,7 @@
 import { h } from 'preact';
 import { Button } from '../../components/molecules/Button';
 import styles from './Sidebar.module.css';
-import { messageBus, events } from '../../services/messageBus';
+import { messageBus, events } from '../../services/message-bus.service';
 import { useEffect, useState } from 'preact/hooks';
 
 export function Sidebar() {
*(Note: Strictly judge the developer's behavior based on the '+' lines to avoid penalizing them for legacy code).*

Please analyze the data above and output the evaluation in the required JSON format.