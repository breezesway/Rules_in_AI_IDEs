diff --git a/app/panel/src/services/maksi.ts b/app/panel/src/services/maksi.ts
index d061165..43a7864 100644
--- a/app/panel/src/services/maksi.ts
+++ b/app/panel/src/services/maksi.ts
@@ -101,6 +101,11 @@ export async function searchMaksiByRFID(rfid: string): Promise<{ hits: MaksiUser
       throw new Error('network_timeout');
     }
     
+    // Handle Node.js fetch errors (TypeError: fetch failed)
+    if (error.name === 'TypeError' && error.message === 'fetch failed') {
+      throw new Error('network_error');
+    }
+    
     // Handle network errors
     if (error.code === 'ENOTFOUND' || error.code === 'ECONNREFUSED' || error.code === 'ETIMEDOUT') {
       throw new Error('network_error');
