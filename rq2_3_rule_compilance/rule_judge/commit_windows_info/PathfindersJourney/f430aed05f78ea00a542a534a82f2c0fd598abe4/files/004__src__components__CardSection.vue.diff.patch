diff --git a/src/components/CardSection.vue b/src/components/CardSection.vue
index eadb0fe..87c272a 100644
--- a/src/components/CardSection.vue
+++ b/src/components/CardSection.vue
@@ -1,4 +1,5 @@
 <template>
+  <ComponentWrapper componentName="CardSection">
     <div class="card-section" :class="{ 'card-section--expanded': expanded }">
       <div v-if="title" class="card-section__header" @click="toggleExpanded">
         <h3 class="card-section__title">{{ title }}</h3>
@@ -20,146 +21,151 @@
         </slot>
       </div>
     </div>
-  </template>
-  
-  <script lang="ts">
-  import { defineComponent, ref, useSlots, computed } from 'vue';
-  
-  export default defineComponent({
-    name: 'CardSection',
-    props: {
-      title: {
-        type: String,
-        default: ''
-      },
-      layout: {
-        type: String,
-        default: 'grid',
-        validator: (value: string) => ['grid', 'horizontal', 'vertical'].includes(value)
-      },
-      collapsible: {
-        type: Boolean,
-        default: false
-      },
-      initiallyExpanded: {
-        type: Boolean,
-        default: true
-      }
+  </ComponentWrapper>
+</template>
+
+<script lang="ts">
+import { defineComponent, ref, useSlots, computed } from 'vue';
+import { ComponentWrapper } from '@/components/common';
+
+export default defineComponent({
+  name: 'CardSection',
+  components: {
+    ComponentWrapper
+  },
+  props: {
+    title: {
+      type: String,
+      default: ''
     },
-    setup(props) {
-      const expanded = ref(props.initiallyExpanded);
-      const slots = useSlots();
-      
-      const hasChildren = computed(() => {
-        return !!slots.default && slots.default().length > 0;
-      });
-      
-      const toggleExpanded = () => {
-        if (props.collapsible) {
-          expanded.value = !expanded.value;
-        }
-      };
-      
-      return {
-        expanded,
-        hasChildren,
-        toggleExpanded
-      };
+    layout: {
+      type: String,
+      default: 'grid',
+      validator: (value: string) => ['grid', 'horizontal', 'vertical'].includes(value)
+    },
+    collapsible: {
+      type: Boolean,
+      default: false
+    },
+    initiallyExpanded: {
+      type: Boolean,
+      default: true
     }
-  });
-  </script>
-  
-  <style lang="scss" scoped>
-  .card-section {
-    width: 100%;
-    margin-bottom: 16px;
+  },
+  setup(props) {
+    const expanded = ref(props.initiallyExpanded);
+    const slots = useSlots();
     
-    &__header {
-      display: flex;
-      justify-content: space-between;
-      align-items: center;
-      margin-bottom: 12px;
-      padding-bottom: 8px;
-      border-bottom: 2px solid rgba(140, 120, 81, 0.3);
-      
-      &:hover {
-        cursor: pointer;
+    const hasChildren = computed(() => {
+      return !!slots.default && slots.default().length > 0;
+    });
+    
+    const toggleExpanded = () => {
+      if (props.collapsible) {
+        expanded.value = !expanded.value;
       }
-    }
+    };
     
-    &__title {
-      margin: 0;
-      color: #5a3e2b;
-      font-family: 'Cinzel', serif;
-    }
+    return {
+      expanded,
+      hasChildren,
+      toggleExpanded
+    };
+  }
+});
+</script>
+
+<style lang="scss" scoped>
+.card-section {
+  width: 100%;
+  margin-bottom: 16px;
+  
+  &__header {
+    display: flex;
+    justify-content: space-between;
+    align-items: center;
+    margin-bottom: 12px;
+    padding-bottom: 8px;
+    border-bottom: 2px solid rgba(140, 120, 81, 0.3);
     
-    &__toggle {
-      background: none;
-      border: none;
-      font-size: 1rem;
-      color: #5a3e2b;
+    &:hover {
       cursor: pointer;
-      transition: transform 0.3s;
     }
+  }
+  
+  &__title {
+    margin: 0;
+    color: #5a3e2b;
+    font-family: 'Cinzel', serif;
+  }
+  
+  &__toggle {
+    background: none;
+    border: none;
+    font-size: 1rem;
+    color: #5a3e2b;
+    cursor: pointer;
+    transition: transform 0.3s;
+  }
+  
+  &__content {
+    transition: max-height 0.3s ease, opacity 0.3s ease;
     
-    &__content {
-      transition: max-height 0.3s ease, opacity 0.3s ease;
-      
-      &--grid {
-        display: grid;
-        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
-        gap: 16px;
-      }
-      
-      &--horizontal {
-        display: flex;
-        flex-wrap: nowrap;
-        gap: 16px;
-        overflow-x: auto;
-        padding-bottom: 12px;
-        
-        > * {
-          flex: 0 0 auto;
-          width: 200px;
-        }
-      }
-      
-      &--vertical {
-        display: flex;
-        flex-direction: column;
-        gap: 16px;
-      }
+    &--grid {
+      display: grid;
+      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
+      gap: 16px;
+    }
+    
+    &--horizontal {
+      display: flex;
+      flex-wrap: nowrap;
+      gap: 16px;
+      overflow-x: auto;
+      padding-bottom: 12px;
       
-      &--empty {
-        min-height: 100px;
-        display: flex;
-        align-items: center;
-        justify-content: center;
+      > * {
+        flex: 0 0 auto;
+        width: 200px;
       }
     }
     
-    &__empty {
-      color: #8c7851;
-      font-style: italic;
-      text-align: center;
-      padding: 24px;
-      background-color: rgba(240, 230, 210, 0.3);
-      border-radius: 8px;
-      width: 100%;
+    &--vertical {
+      display: flex;
+      flex-direction: column;
+      gap: 16px;
     }
     
-    &--expanded {
-      .card-section__toggle {
-        transform: rotate(180deg);
-      }
+    &--empty {
+      min-height: 100px;
+      display: flex;
+      align-items: center;
+      justify-content: center;
     }
   }
   
-  @media (max-width: 768px) {
-    .card-section {
-      &__content--grid {
-        grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
-      }
+  &__empty {
+    color: #8c7851;
+    font-style: italic;
+    text-align: center;
+    padding: 24px;
+    background-color: rgba(240, 230, 210, 0.3);
+    border-radius: 8px;
+    width: 100%;
+  }
+  
+  &--expanded {
+    .card-section__toggle {
+      transform: rotate(180deg);
+    }
+  }
+}
+
+@media (max-width: 768px) {
+  .card-section {
+    &__content--grid {
+      grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
     }
   }
-  </style>
\ No newline at end of file
+}
+</style>
\ No newline at end of file
