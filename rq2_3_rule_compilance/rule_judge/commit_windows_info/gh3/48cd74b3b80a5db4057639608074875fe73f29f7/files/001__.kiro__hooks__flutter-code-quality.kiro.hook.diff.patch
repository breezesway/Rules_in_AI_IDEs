diff --git a/.kiro/hooks/flutter-code-quality.kiro.hook b/.kiro/hooks/flutter-code-quality.kiro.hook
index 3fc035a..de4f450 100644
--- a/.kiro/hooks/flutter-code-quality.kiro.hook
+++ b/.kiro/hooks/flutter-code-quality.kiro.hook
@@ -4,7 +4,7 @@
   "description": "Automatically runs flutter analyze and dart format when Dart files are modified to maintain code quality and formatting standards",
   "version": "1",
   "when": {
-    "type": "fileEdited",
+    "type": "userTriggered",
     "patterns": [
       "lib/**/*.dart",
       "test/**/*.dart"
@@ -12,6 +12,6 @@
   },
   "then": {
     "type": "askAgent",
-    "prompt": "Run `flutter analyze` to check for any code issues, then run `dart format .` to format all Dart files in the project. Report the results of both commands."
+    "prompt": "Run `flutter analyze --fatal-infos --fatal-warnings` to check for any code issues with strict quality enforcement (warnings and info messages will cause failure), then run `dart format .` to format all Dart files in the project. Report the results of both commands and ensure no warnings or info messages are present."
   }
 }
\ No newline at end of file
