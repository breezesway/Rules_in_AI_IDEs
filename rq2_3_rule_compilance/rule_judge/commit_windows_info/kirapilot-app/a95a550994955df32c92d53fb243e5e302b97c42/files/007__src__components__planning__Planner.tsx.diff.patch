diff --git a/src/components/planning/Planner.tsx b/src/components/planning/Planner.tsx
index 5c2e096..772499a 100644
--- a/src/components/planning/Planner.tsx
+++ b/src/components/planning/Planner.tsx
@@ -51,7 +51,9 @@ export function Planner({ viewMode = 'week' }: PlanningScreenProps) {
     console.log('Moving task:', { taskId, fromColumn, toColumn, date });
 
     const task = tasks.find(t => t.id === taskId);
-    if (!task) return;
+    if (!task) {
+      return;
+    }
 
     let newScheduledDate: Date | undefined;
 
