diff --git a/app/pages/playground/index.vue b/app/pages/playground/index.vue
index f7d36c1..176b839 100644
--- a/app/pages/playground/index.vue
+++ b/app/pages/playground/index.vue
@@ -7,38 +7,50 @@ const items = routes.filter((route) => route.path.startsWith("/playground/"));
 const getItemName = (path) => {
   return path.split("/").pop();
 };
+
+const search = ref("");
+const filteredItems = computed(() => {
+  const filtered = items.filter((item) =>
+    getItemName(item.path).toLowerCase().includes(search.value.toLowerCase())
+  );
+
+  return [...filtered].sort((a, b) => {
+    return getItemName(a.path).localeCompare(getItemName(b.path));
+  });
+});
 </script>
 <template>
-  <div class="container mx-auto px-4 py-8">
-    <div class="max-w-4xl mx-auto">
-      <div class="mb-8">
-        <h1 class="text-3xl font-bold tracking-tight">Playground</h1>
-        <p class="text-muted-foreground mt-2">
-          Explore and test different features and components
-        </p>
+  <div>
+    <div class="mb-4">
+      <h1 class="text-3xl font-bold tracking-tight">Playground</h1>
+      <p class="text-muted-foreground mt-2">
+        Explore and test different features and components
+      </p>
+      <div class="flex items-center gap-2 mt-4">
+        <Input v-model="search" placeholder="Search" />
       </div>
+    </div>
 
-      <div class="grid gap-4">
-        <Card
-          v-for="item in items"
-          :key="item.name"
-          class="hover:shadow-md transition-shadow"
-        >
-          <CardHeader class="pb-3">
-            <CardTitle class="text-lg">
-              <NuxtLink
-                :to="item.path"
-                class="text-primary hover:text-primary/80 transition-colors"
-              >
-                {{ getItemName(item.path) }}
-              </NuxtLink>
-            </CardTitle>
-            <CardDescription class="text-sm font-mono">
-              {{ item.path }}
-            </CardDescription>
-          </CardHeader>
-        </Card>
-      </div>
+    <div class="grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
+      <Card
+        v-for="item in filteredItems"
+        :key="item.name"
+        class="hover:shadow-md transition-shadow"
+      >
+        <CardHeader class="pb-3">
+          <CardTitle class="text-lg">
+            <NuxtLink
+              :to="item.path"
+              class="text-primary hover:text-primary/80 transition-colors"
+            >
+              {{ getItemName(item.path) }}
+            </NuxtLink>
+          </CardTitle>
+          <CardDescription class="text-sm font-mono">
+            {{ item.path }}
+          </CardDescription>
+        </CardHeader>
+      </Card>
     </div>
   </div>
 </template>
