diff --git a/src/utils/services/configuration-service.ts b/src/utils/services/configuration-service.ts
index 1d8696a..ebe745a 100644
--- a/src/utils/services/configuration-service.ts
+++ b/src/utils/services/configuration-service.ts
@@ -74,10 +74,37 @@ export class ConfigurationService {
 
   /**
    * Gets the complete configuration object
-   * @returns The complete TestConfiguration object
+   * @returns A deep copy of the TestConfiguration object to prevent modification
    */
   getConfiguration(): TestConfiguration {
-    return this.config;
+    return this.deepClone(this.config);
+  }
+
+  /**
+   * Deep clone function that preserves RegExp objects
+   * @param obj The object to clone
+   * @returns A deep copy of the object
+   */
+  private deepClone<T>(obj: T): T {
+    if (obj === null || typeof obj !== 'object') {
+      return obj;
+    }
+
+    if (obj instanceof RegExp) {
+      return new RegExp(obj.source, obj.flags) as T;
+    }
+
+    if (Array.isArray(obj)) {
+      return obj.map(item => this.deepClone(item)) as T;
+    }
+
+    const cloned = {} as T;
+    for (const key in obj) {
+      if (obj.hasOwnProperty(key)) {
+        cloned[key] = this.deepClone(obj[key]);
+      }
+    }
+    return cloned;
   }
 
   /**
