diff --git a/src/components/results/ScoreHero/ScoreHero.tsx b/src/components/results/ScoreHero/ScoreHero.tsx
index 58d2d2b..aba9553 100644
--- a/src/components/results/ScoreHero/ScoreHero.tsx
+++ b/src/components/results/ScoreHero/ScoreHero.tsx
@@ -27,32 +27,32 @@ const formatDate = (dateStr?: string): string => {
     month: '2-digit',
     year: 'numeric',
     hour: '2-digit',
-    minute: '2-digit'
+    minute: '2-digit',
   });
 };
 
-export const ScoreHero = ({ 
-  score, 
-  maxScore = 100, 
-  url, 
-  date, 
+export const ScoreHero = ({
+  score,
+  maxScore = 100,
+  url,
+  date,
   previousScore,
   totalModules,
-  completedModules
+  completedModules,
 }: ScoreHeroProps) => {
   const scoreLabel = getScoreLabel(score);
   const formattedDate = formatDate(date);
   const status = getStatusFromScore(score);
-  
+
   // Bepaal de tekstkleur op basis van status
   const textColorClasses = {
     success: 'text-green-600',
     warning: 'text-orange-600',
-    danger: 'text-red-600'
+    danger: 'text-red-600',
   };
 
   return (
-    <motion.div 
+    <motion.div
       className="score-hero"
       initial={{ opacity: 0, y: 20 }}
       animate={{ opacity: 1, y: 0 }}
@@ -65,39 +65,33 @@ export const ScoreHero = ({
 
         <div className="text-center">
           <h2 className={`text-2xl font-bold ${textColorClasses[status]}`}>{scoreLabel}</h2>
-          
+
           {previousScore !== undefined && (
             <p className="text-sm text-gray-600 mt-1">
               Vorige score: {previousScore}%
               {score > previousScore && (
-                <span className="ml-2 text-green-600 font-medium">
-                  ↑ {score - previousScore}%
-                </span>
+                <span className="ml-2 text-green-600 font-medium">↑ {score - previousScore}%</span>
               )}
               {score < previousScore && (
-                <span className="ml-2 text-red-600 font-medium">
-                  ↓ {previousScore - score}%
-                </span>
+                <span className="ml-2 text-red-600 font-medium">↓ {previousScore - score}%</span>
               )}
             </p>
           )}
-          
+
           {totalModules !== undefined && completedModules !== undefined && (
             <p className="text-sm text-gray-600 mt-2">
-              <span className="font-medium">{completedModules} van {totalModules} modules</span>
+              <span className="font-medium">
+                {completedModules} van {totalModules} modules
+              </span>
             </p>
           )}
         </div>
 
         <div className="text-center">
           <p className="text-sm font-medium text-gray-700">{url}</p>
-          {date && (
-            <p className="text-xs text-gray-500 mt-1">
-              Gescand op: {formattedDate}
-            </p>
-          )}
+          {date && <p className="text-xs text-gray-500 mt-1">Gescand op: {formattedDate}</p>}
         </div>
       </div>
     </motion.div>
   );
-}; 
\ No newline at end of file
+};
