diff --git a/src/App.tsx b/src/App.tsx
index 9ed14a3..108b9fa 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,9 +1,11 @@
 import React, { useState } from 'react';
-import { FileText, Image } from 'lucide-react';
+import { FileText, Image, History, Settings } from 'lucide-react';
 import { Toaster } from 'sonner';
 import { FeatureCard } from './ui/feature-card';
 import { PdfMerger } from './components/pdf-merger';
 import { ImageCompressor } from './components/image-compressor';
+import { OperationHistory } from './components/operation-history';
+import { AppSettings } from './components/app-settings';
 
 export default function App() {
   const [activeModal, setActiveModal] = useState<string | null>(null);
@@ -12,8 +14,29 @@ export default function App() {
     <div className="min-h-screen bg-gray-50 p-6">
       <div className="container mx-auto max-w-6xl">
         <div className="mb-8">
+          <div className="flex items-center justify-between">
+            <div>
           <h1 className="text-3xl font-bold text-gray-900 mb-2">工具集</h1>
           <p className="text-gray-600">选择一个工具开始使用</p>
+            </div>
+            <div className="flex space-x-3">
+              <button
+                onClick={() => setActiveModal('operation-history')}
+                className="flex items-center space-x-2 px-4 py-2 bg-white border border-gray-300 rounded-xl hover:bg-gray-50 font-medium transition-colors duration-200 shadow-sm"
+              >
+                <History className="h-4 w-4" />
+                <span>操作历史</span>
+              </button>
+              
+              <button
+                onClick={() => setActiveModal('app-settings')}
+                className="flex items-center space-x-2 px-4 py-2 bg-white border border-gray-300 rounded-xl hover:bg-gray-50 font-medium transition-colors duration-200 shadow-sm"
+              >
+                <Settings className="h-4 w-4" />
+                <span>设置</span>
+              </button>
+            </div>
+          </div>
         </div>
 
         <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
@@ -41,6 +64,16 @@ export default function App() {
           isOpen={activeModal === 'image-compressor'}
           onClose={() => setActiveModal(null)}
         />
+
+        <OperationHistory
+          isOpen={activeModal === 'operation-history'}
+          onClose={() => setActiveModal(null)}
+        />
+
+        <AppSettings
+          isOpen={activeModal === 'app-settings'}
+          onClose={() => setActiveModal(null)}
+        />
         
         {/* Toast 通知组件 */}
         <Toaster 
