diff --git a/src/main.ts b/src/main.ts
index 8a8c3a8..9c902fa 100644
--- a/src/main.ts
+++ b/src/main.ts
@@ -109,17 +109,21 @@ const historyManager = new HistoryManager();
 // 应用配置管理
 interface AppConfig {
   autoNumberDuplicateFiles: boolean;
+  defaultSaveLocation: string;
 }
 
 class ConfigManager {
   private configFilePath: string;
   private defaultConfig: AppConfig = {
-    autoNumberDuplicateFiles: true
+    autoNumberDuplicateFiles: true,
+    defaultSaveLocation: ''
   };
 
   constructor() {
     const userDataPath = app.getPath('userData');
     this.configFilePath = path.join(userDataPath, 'app-config.json');
+    // 设置默认保存位置为下载目录
+    this.defaultConfig.defaultSaveLocation = app.getPath('downloads');
   }
 
   async getConfig(): Promise<AppConfig> {
