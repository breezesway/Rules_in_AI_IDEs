diff --git a/src/components/image-compressor.tsx b/src/components/image-compressor.tsx
index ace3685..7ba3235 100644
--- a/src/components/image-compressor.tsx
+++ b/src/components/image-compressor.tsx
@@ -16,12 +16,7 @@ interface ImageFile {
   status: 'ready' | 'processing' | 'done' | 'error';
 }
 
-interface CompressionProgress {
-  currentFile: string;
-  currentIndex: number;
-  totalFiles: number;
-  percentage: number;
-}
+
 
 export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
   const [files, setFiles] = useState<ImageFile[]>([]);
@@ -33,7 +28,19 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
   const [namingSuffix, setNamingSuffix] = useState('_compressed');
   const [saveLocation, setSaveLocation] = useState('');
   const [isProcessing, setIsProcessing] = useState(false);
-  const [progress, setProgress] = useState<CompressionProgress | null>(null);
+
+  // 初始化保存位置
+  React.useEffect(() => {
+    if (isOpen && !saveLocation) {
+      window.electronAPI.getAppConfig().then(config => {
+        if (config.defaultSaveLocation) {
+          setSaveLocation(config.defaultSaveLocation);
+        }
+      }).catch(err => {
+        console.error('获取应用配置失败:', err);
+      });
+    }
+  }, [isOpen, saveLocation]);
 
   const handleFileSelect = async () => {
     try {
@@ -57,28 +64,12 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
     setFiles(prev => prev.filter((_, i) => i !== index));
   };
 
-  const handleSelectSaveLocation = async () => {
-    try {
-      const result = await window.electronAPI.selectSaveLocation();
-      if (result.success && result.path) {
-        setSaveLocation(result.path);
-      }
-    } catch (error) {
-      console.error('选择保存位置失败:', error);
-      toast.error('选择保存位置失败，请重试');
-    }
-  };
+
 
   const handleCompress = async () => {
     if (files.length === 0) return;
 
     setIsProcessing(true);
-    setProgress({
-      currentFile: '',
-      currentIndex: 0,
-      totalFiles: files.length,
-      percentage: 0
-    });
 
     try {
       const settings = {
@@ -108,7 +99,6 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
       toast.error('压缩失败，请重试');
     } finally {
       setIsProcessing(false);
-      setProgress(null);
     }
   };
 
@@ -124,10 +114,10 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
       </button>
       <button
         onClick={handleCompress}
-        disabled={files.length === 0}
+        disabled={files.length === 0 || isProcessing}
         className="px-6 py-3 bg-gray-900 text-white rounded-xl font-medium hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-200"
       >
-        压缩图片
+        {isProcessing ? '压缩中...' : '压缩图片'}
       </button>
     </div>
   );
@@ -147,7 +137,8 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
             </div>
             <button
               onClick={handleFileSelect}
-              className="bg-gray-900 text-white px-6 py-3 rounded-xl font-medium hover:bg-gray-800 transition-colors duration-200"
+              disabled={isProcessing}
+              className="bg-gray-900 text-white px-6 py-3 rounded-xl font-medium hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-200"
             >
               选择文件
             </button>
@@ -312,19 +303,18 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
             </div>
           </div>
 
-          {/* 保存位置 */}
+          {/* 保存位置信息 */}
           <div>
             <label className="block text-sm font-medium mb-2 text-gray-900">保存位置</label>
-            <div className="flex space-x-3">
-              <input
-                type="text"
-                value="默认下载目录"
-                readOnly
-                className="flex-1 border border-gray-300 rounded-xl px-4 py-3 bg-gray-50 text-gray-600"
-              />
-              <button className="px-6 py-3 border border-gray-300 rounded-xl hover:bg-gray-50 font-medium transition-colors duration-200 whitespace-nowrap">
-                浏览
-              </button>
+            <div className="p-3 bg-gray-50 border border-gray-200 rounded-xl">
+              {saveLocation ? (
+                <p className="text-sm text-gray-700">将保存到: {saveLocation}</p>
+              ) : (
+                <p className="text-sm text-gray-700">将使用应用设置中配置的默认保存位置</p>
+              )}
+              <p className="text-xs text-gray-500 mt-1">
+                可在应用设置中修改默认保存位置
+              </p>
             </div>
           </div>
         </div>
