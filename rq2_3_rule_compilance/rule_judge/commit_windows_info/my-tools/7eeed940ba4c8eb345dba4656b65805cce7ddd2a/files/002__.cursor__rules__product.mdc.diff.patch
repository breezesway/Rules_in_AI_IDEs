diff --git a/.cursor/rules/product.mdc b/.cursor/rules/product.mdc
index 83a4498..fc181bc 100644
--- a/.cursor/rules/product.mdc
+++ b/.cursor/rules/product.mdc
@@ -3,6 +3,12 @@ description:
 globs: 
 alwaysApply: false
 ---
+---
+description: 产品需求和设计问题
+globs: 
+alwaysApply: false
+---
+
 # Electron 工具集产品规范
 
 ## 🎯 产品目标
@@ -116,12 +122,84 @@ alwaysApply: false
   - 具体的解决方案建议
   - 错误不影响其他功能正常使用
 
+## 🎨 主题系统产品规范
+
+### 主题模式定义
+- **浅色模式（Light）**：
+  - 白色主背景，深色文字
+  - 适合光线充足的环境使用
+  - 经典的办公软件风格
+- **深色模式（Dark）**：
+  - 深灰色主背景，浅色文字
+  - 适合弱光环境，减少眼疲劳
+  - 现代化的界面设计风格
+- **跟随系统（System）**：
+  - 自动检测系统主题设置
+  - 实时响应系统主题变化
+  - 无需手动切换，智能适配
+
+### 深色模式设计规范
+- **颜色层次**：
+  - 主背景：深灰色（#111827）
+  - 次级背景：中灰色（#1F2937）
+  - 三级背景：浅灰色（#374151）
+  - 主要文字：浅灰色（#F9FAFB）
+  - 次要文字：中浅灰色（#D1D5DB）
+  - 辅助文字：中灰色（#9CA3AF）
+- **状态颜色**：
+  - 成功状态：绿色系，使用半透明背景
+  - 错误状态：红色系，使用半透明背景
+  - 警告状态：黄色系，使用半透明背景
+  - 信息状态：蓝色系，使用半透明背景
+- **交互元素**：
+  - 按钮：保持足够的对比度
+  - 开关：白底设计，在深色背景下更突出
+  - 进度条：使用浅色填充，深色背景
+  - 边框：使用中等灰度，避免过于突兀
+
+### 主题选择器设计
+- **布局结构**：
+  - 三个选项卡式布局，横向排列
+  - 每个选项包含图标、标题、描述
+  - 当前选中状态的视觉反馈
+- **选项内容**：
+  - 浅色模式：太阳图标，"浅色"标题，"适合光线充足的环境"描述
+  - 深色模式：月亮图标，"深色"标题，"适合弱光环境，保护视力"描述
+  - 跟随系统：显示器图标，"跟随系统"标题，"自动跟随系统主题设置"描述
+- **交互反馈**：
+  - 点击时立即切换主题
+  - 显示切换成功的Toast提示
+  - 防止重复点击的加载状态
+
+### 主题切换用户体验
+- **切换时机**：
+  - 用户主动在设置中切换
+  - 系统主题变化时自动切换（跟随系统模式下）
+  - 应用启动时根据保存的偏好设置
+- **切换过程**：
+  - 立即生效，无闪烁或延迟
+  - 所有界面元素同步更新
+  - 保持用户当前的操作状态
+- **状态保持**：
+  - 主题偏好保存到配置文件
+  - 应用重启后保持用户选择
+  - 系统跟随模式下记录用户偏好
+
+
+
 ## 🔧 应用级功能
 
 ### 应用设置
 - **文件保存配置**：
   - **默认保存位置**：统一设置所有工具的默认保存位置
   - **文件重名处理**：选择自动编号或询问覆盖
+- **主题设置**：
+  - **主题模式**：支持浅色模式、深色模式和跟随系统三种模式
+  - **实时切换**：主题切换即时生效，无需重启应用
+  - **主题持久化**：主题偏好自动保存到配置文件，重启后保持
+  - **系统跟随**：支持自动跟随系统主题变化，实时响应
+  - **选择界面**：提供可视化的主题选择器，包含图标和描述
+  - **状态反馈**：主题切换时提供明确的成功提示
 - **设置界面**：
   - 模态框形式，分区域显示配置选项
   - 实时生效，无需重启应用
@@ -162,6 +240,7 @@ alwaysApply: false
 - **界面清晰**：信息层次分明，操作路径简单
 - **零干扰**：专注核心功能，避免不必要的弹窗
 - **一致性**：所有工具保持一致的操作流程
+- **主题适配**：所有界面完整支持浅色和深色模式
 
 ### 功能设计
 - **容错性**：友好的错误处理和恢复机制
@@ -169,6 +248,13 @@ alwaysApply: false
 - **渐进增强**：从基础功能开始，逐步优化用户体验
 - **配置集中**：应用级别配置，工具级别执行
 
+### 主题系统设计原则
+- **无缝切换**：主题切换立即生效，无需重启应用
+- **智能跟随**：支持跟随系统主题自动切换
+- **视觉协调**：深色模式下保持良好的对比度和可读性
+- **状态一致**：所有组件状态在不同主题下保持视觉一致性
+- **用户友好**：主题选择界面直观易懂，提供清晰的预览
+
 ## 📊 功能实现状态
 
 ### ✅ 已完成功能
@@ -201,13 +287,15 @@ alwaysApply: false
 - [x] 文件重名处理配置
 - [x] Toast通知系统
 - [x] 配置实时同步
+- [x] 主题切换系统（浅色/深色/跟随系统）
 
 ### 🔄 优化改进项
 
 #### 用户体验优化
 - [ ] 拖拽文件上传支持
 - [ ] 更多键盘快捷键
-- [ ] 主题切换（暗黑模式）
+- [ ] 主题切换动画效果
+- [ ] 深色模式下的微调优化
 
 #### 功能扩展
 - [ ] 更多文件格式支持
