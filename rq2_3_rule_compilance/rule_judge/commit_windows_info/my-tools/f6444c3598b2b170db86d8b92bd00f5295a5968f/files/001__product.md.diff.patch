diff --git a/product.md b/product.md
index 3d6bcef..8c50da5 100644
--- a/product.md
+++ b/product.md
@@ -218,8 +218,10 @@
 - **文件选择方式统一**：沿用 PDF 合并的成功经验
   - **原因**：Electron 原生对话框比浏览器文件选择更可靠
   - **实现**：`dialog.showOpenDialog` 支持图片格式过滤
-- **压缩算法选择**：优先实现按质量等级压缩
-  - **原因**：用户最常用的压缩方式，技术实现相对简单
+- **压缩算法优化**：从简单质量压缩到智能迭代压缩
+  - **按质量等级压缩**：用户最常用的压缩方式，支持10%-100%质量调节
+  - **按文件大小压缩**：智能迭代算法，精确逼近目标文件大小
+  - **按图片尺寸压缩**：保持原始比例的尺寸调整
 
 ### 关键技术实现
 - **Sharp 动态加载**：
@@ -238,13 +240,20 @@
     }
   });
   ```
+- **智能按文件大小压缩算法**：
+  - **格式特性优化**：针对JPEG、PNG、WebP不同格式的压缩特性调整初始质量估算
+  - **迭代逼近算法**：最多5次迭代，10%容差范围内精确控制文件大小
+  - **质量自适应调整**：根据实际压缩效果动态调整质量参数
+  - **临时文件管理**：使用临时文件测试压缩效果，自动清理
 - **多格式支持**：JPEG、PNG、WebP 的质量压缩适配
 - **文件处理流程**：
-  1. 文件选择 → 2. 格式检测 → 3. 质量压缩 → 4. 格式转换 → 5. 保存文件
+  1. 文件选择 → 2. 格式检测 → 3. 智能压缩 → 4. 格式转换 → 5. 保存文件
 
 ### 压缩功能特性
-- **按质量等级压缩**：10%-100% 可调节质量滑块
-- **格式转换支持**：保持原格式或转换为 JPG/PNG/WebP
+- **按质量等级压缩**：10%-100% 可调节质量滑块，高质量时智能防止文件变大
+- **按文件大小压缩**：精确控制输出文件大小，智能迭代逼近目标大小
+- **按图片尺寸压缩**：调整图片宽度和高度，保持原始比例
+- **格式转换支持**：保持原格式或转换为 JPG/PNG/WebP，针对不同格式优化压缩参数
 - **文件名后缀自定义**：默认添加 "_compressed" 后缀
 - **重名文件处理**：集成应用配置，支持自动编号或询问覆盖
 - **批量处理**：支持多文件同时压缩
@@ -340,6 +349,62 @@
   - 统一配置加载失败的处理逻辑
   - 提供优雅的降级方案
 
+## 按文件大小压缩功能深度优化（2024-12-29）
+
+### 优化目标
+- **精确控制**：将输出文件大小控制在目标大小的±10%范围内
+- **格式适配**：针对JPEG、PNG、WebP不同格式的压缩特性优化算法
+- **性能平衡**：在压缩精度和处理速度之间找到最佳平衡点
+
+### 算法设计
+- **智能初始估算**：
+  - JPEG格式：压缩效率高，使用 `compressionRatio * 120` 估算
+  - PNG格式：压缩效率较低，使用 `compressionRatio * 100` 估算  
+  - WebP格式：压缩效率最高，使用 `compressionRatio * 130` 估算
+- **迭代优化策略**：
+  - 最多5次迭代，避免过度处理
+  - 10%容差范围，平衡精度和性能
+  - 动态质量调整，根据实际效果智能修正
+- **临时文件管理**：
+  - 使用临时文件测试压缩效果
+  - 自动清理临时文件，避免磁盘空间浪费
+  - 达到目标时直接重命名，提高效率
+
+### 技术实现细节
+```typescript
+// 格式特性优化的质量估算
+if (outputFormat === 'jpeg' || outputFormat === 'jpg') {
+  estimatedQuality = Math.max(20, Math.min(85, Math.round(compressionRatio * 120)));
+} else if (outputFormat === 'png') {
+  estimatedQuality = Math.max(10, Math.min(80, Math.round(compressionRatio * 100)));
+} else if (outputFormat === 'webp') {
+  estimatedQuality = Math.max(25, Math.min(90, Math.round(compressionRatio * 130)));
+}
+
+// 迭代优化逻辑
+while (attempts < maxAttempts) {
+  // 生成测试文件并检查大小
+  const sizeRatio = testSize / targetSizeBytes;
+  if (sizeRatio >= (1 - tolerance) && sizeRatio <= (1 + tolerance)) {
+    // 达到目标大小，直接使用
+    break;
+  }
+  // 智能调整质量参数...
+}
+```
+
+### 性能优化
+- **早期终止**：达到目标大小时立即停止迭代
+- **收敛检测**：连续两次压缩结果相近时停止迭代
+- **资源清理**：及时清理临时文件，避免资源泄漏
+- **错误恢复**：单次迭代失败不影响整体流程
+
+### 用户体验提升
+- **更精确的文件大小控制**：输出文件大小更接近用户设定的目标
+- **更好的格式适配**：不同图片格式都能获得最佳的压缩效果
+- **更快的处理速度**：智能算法减少不必要的迭代次数
+- **更稳定的结果**：减少压缩失败和异常情况
+
 ## 重要Bug修复记录
 
 ### 图片压缩质量问题修复（2024-12-29）
@@ -387,6 +452,41 @@
 - **影响范围**：图片压缩工具和PDF合并工具都进行了相同修复
 - **用户体验改进**：设置修改后立即生效，无需重启应用
 
+### 交互体验统一优化（2024-12-29）
+- **问题描述**：PDF合并和图片压缩两个工具的操作完成后交互行为不一致
+  - PDF合并：成功后只重置表单，弹框保持打开状态
+  - 图片压缩：成功后自动关闭弹框
+- **优化目标**：统一两个工具的交互体验，提供一致的用户操作流程
+- **修复方案**：
+  - 修改PDF合并组件的成功处理逻辑
+  - 移除文件状态更新和延迟重置的复杂逻辑
+  - 成功后立即重置表单并关闭弹框，与图片压缩保持一致
+- **技术实现**：
+  ```typescript
+  // 修复前：复杂的状态管理
+  if (result?.success) {
+    setFiles(prev => prev.map(f => 
+      f.status === 'ready' ? { ...f, status: 'done' as const } : f
+    ));
+    setProgress(100);
+    toast.success('PDF合并完成！');
+    setTimeout(() => {
+      resetForm();
+    }, 500);
+  }
+
+  // 修复后：简洁统一的处理
+  if (result?.success) {
+    toast.success('PDF合并完成！');
+    resetForm();
+    onClose(); // 自动关闭弹框
+  }
+  ```
+- **用户体验改进**：
+  - 操作完成后自动返回主界面，减少用户操作步骤
+  - 两个工具的操作流程完全一致，降低学习成本
+  - 简化界面状态管理，提高应用响应速度
+
 # 应用级功能
 
 ## 操作历史记录
@@ -453,14 +553,16 @@
 ### 图片压缩工具
 - [x] **文件选择**：Electron 原生对话框，支持图片格式过滤
 - [x] **按质量压缩**：10%-100% 可调节质量滑块，包含高质量文件大小保护机制
-- [x] **按文件大小压缩**：智能估算质量级别，达到目标文件大小
+- [x] **按文件大小压缩**：智能迭代算法，精确逼近目标文件大小（±10%容差）
 - [x] **按图片尺寸压缩**：调整图片宽度和高度，保持原始比例
-- [x] **格式转换**：支持 JPEG、PNG、WebP 格式转换，正确处理各格式压缩参数
+- [x] **格式转换**：支持 JPEG、PNG、WebP 格式转换，针对不同格式优化压缩参数
 - [x] **批量处理**：支持多文件同时压缩
 - [x] **输出设置**：自定义文件名后缀，保存位置使用应用级别配置
 - [x] **错误容错**：单文件失败不影响其他文件
 - [x] **原生模块集成**：Sharp 库动态加载和 Vite 配置优化
 - [x] **智能质量控制**：高质量设置时防止文件变大，自动调整压缩参数
+- [x] **格式特性优化**：针对JPEG、PNG、WebP不同压缩特性的算法优化
+- [x] **迭代压缩算法**：最多5次迭代，智能收敛检测，临时文件管理
 
 ### 应用级功能
 - [x] **操作历史**：完整的历史记录管理系统
