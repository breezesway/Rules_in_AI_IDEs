diff --git a/src/components/image-compressor.tsx b/src/components/image-compressor.tsx
index 24a26b1..ace3685 100644
--- a/src/components/image-compressor.tsx
+++ b/src/components/image-compressor.tsx
@@ -1,34 +1,55 @@
 import React, { useState } from 'react';
 import { Image, FolderOpen, Settings } from 'lucide-react';
 import { Modal } from '../ui/modal';
+import { formatFileSize } from '../lib/utils';
+import { toast } from 'sonner';
 
 interface ImageCompressorProps {
   isOpen: boolean;
   onClose: () => void;
 }
 
+interface ImageFile {
+  name: string;
+  path: string;
+  size: number;
+  status: 'ready' | 'processing' | 'done' | 'error';
+}
+
+interface CompressionProgress {
+  currentFile: string;
+  currentIndex: number;
+  totalFiles: number;
+  percentage: number;
+}
+
 export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
-  const [files, setFiles] = useState<File[]>([]);
+  const [files, setFiles] = useState<ImageFile[]>([]);
   const [compressionMethod, setCompressionMethod] = useState('quality'); // 'quality', 'size', 'dimension'
   const [qualityLevel, setQualityLevel] = useState(70);
   const [targetSize, setTargetSize] = useState(500);
   const [maxDimension, setMaxDimension] = useState(1920);
   const [outputFormat, setOutputFormat] = useState('original');
   const [namingSuffix, setNamingSuffix] = useState('_compressed');
+  const [saveLocation, setSaveLocation] = useState('');
+  const [isProcessing, setIsProcessing] = useState(false);
+  const [progress, setProgress] = useState<CompressionProgress | null>(null);
 
-  const handleDrop = (e: React.DragEvent) => {
-    e.preventDefault();
-    const allFiles = Array.from(e.dataTransfer.files) as File[];
-    const droppedFiles = allFiles.filter(
-      (file: File) => file.type.startsWith('image/')
-    );
-    setFiles(prev => [...prev, ...droppedFiles]);
-  };
-
-  const handleFileSelect = (e: React.ChangeEvent<HTMLInputElement>) => {
-    if (e.target.files) {
-      const selectedFiles = Array.from(e.target.files);
-      setFiles(prev => [...prev, ...selectedFiles]);
+  const handleFileSelect = async () => {
+    try {
+      const result = await window.electronAPI.selectImages();
+      if (result.success && result.files) {
+        const imageFiles: ImageFile[] = result.files.map(file => ({
+          name: file.name,
+          path: file.path,
+          size: file.size,
+          status: 'ready' as const
+        }));
+        setFiles(prev => [...prev, ...imageFiles]);
+      }
+    } catch (error) {
+      console.error('选择文件失败:', error);
+      toast.error('选择文件失败，请重试');
     }
   };
 
@@ -36,6 +57,63 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
     setFiles(prev => prev.filter((_, i) => i !== index));
   };
 
+  const handleSelectSaveLocation = async () => {
+    try {
+      const result = await window.electronAPI.selectSaveLocation();
+      if (result.success && result.path) {
+        setSaveLocation(result.path);
+      }
+    } catch (error) {
+      console.error('选择保存位置失败:', error);
+      toast.error('选择保存位置失败，请重试');
+    }
+  };
+
+  const handleCompress = async () => {
+    if (files.length === 0) return;
+
+    setIsProcessing(true);
+    setProgress({
+      currentFile: '',
+      currentIndex: 0,
+      totalFiles: files.length,
+      percentage: 0
+    });
+
+    try {
+      const settings = {
+        method: compressionMethod,
+        quality: qualityLevel,
+        targetSize,
+        maxDimension,
+        outputFormat,
+        namingSuffix,
+        saveLocation: saveLocation || undefined
+      };
+
+      const result = await window.electronAPI.compressImages(
+        files.map(f => f.path),
+        settings
+      );
+
+      if (result.success) {
+        toast.success(`成功压缩 ${result.processedCount} 张图片`);
+        setFiles([]);
+        onClose();
+      } else {
+        toast.error(result.error || '压缩失败');
+      }
+    } catch (error) {
+      console.error('压缩失败:', error);
+      toast.error('压缩失败，请重试');
+    } finally {
+      setIsProcessing(false);
+      setProgress(null);
+    }
+  };
+
+
+
   const footerContent = (
     <div className="flex justify-end space-x-3">
       <button
@@ -45,6 +123,7 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
         关闭
       </button>
       <button
+        onClick={handleCompress}
         disabled={files.length === 0}
         className="px-6 py-3 bg-gray-900 text-white rounded-xl font-medium hover:bg-gray-800 disabled:opacity-50 disabled:cursor-not-allowed transition-colors duration-200"
       >
@@ -56,34 +135,22 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
   return (
     <Modal isOpen={isOpen} onClose={onClose} title="图片压缩" footer={footerContent}>
       <div className="space-y-6">
-        {/* 文件拖拽区域 */}
-        <div
-          className="border-2 border-dashed border-gray-300 rounded-2xl p-12 text-center hover:border-gray-400 hover:bg-gray-50 transition-all duration-200"
-          onDrop={handleDrop}
-          onDragOver={(e) => e.preventDefault()}
-        >
+        {/* 文件选择区域 */}
+        <div className="border-2 border-dashed border-gray-300 rounded-2xl p-12 text-center hover:border-gray-400 hover:bg-gray-50 transition-all duration-200">
           <div className="flex flex-col items-center space-y-4">
             <div className="p-4 bg-gray-100 rounded-2xl">
               <FolderOpen className="h-8 w-8 text-gray-600" />
             </div>
             <div className="space-y-2">
-              <p className="font-medium text-gray-900">拖拽图片文件到此处，或</p>
+              <p className="font-medium text-gray-900">选择图片文件进行压缩</p>
               <p className="text-sm text-gray-500">支持 JPG, PNG, WebP 格式</p>
             </div>
-            <input
-              type="file"
-              accept="image/*"
-              multiple
-              onChange={handleFileSelect}
-              className="hidden"
-              id="image-input"
-            />
-            <label
-              htmlFor="image-input"
-              className="bg-gray-900 text-white px-6 py-3 rounded-xl font-medium hover:bg-gray-800 cursor-pointer transition-colors duration-200"
+            <button
+              onClick={handleFileSelect}
+              className="bg-gray-900 text-white px-6 py-3 rounded-xl font-medium hover:bg-gray-800 transition-colors duration-200"
             >
               选择文件
-            </label>
+            </button>
           </div>
         </div>
 
@@ -103,7 +170,7 @@ export function ImageCompressor({ isOpen, onClose }: ImageCompressorProps) {
                     <div>
                       <p className="font-medium text-gray-900">{file.name}</p>
                       <p className="text-sm text-gray-500">
-                        {(file.size / 1024 / 1024).toFixed(2)} MB
+                        {formatFileSize(file.size)}
                       </p>
                     </div>
                   </div>
